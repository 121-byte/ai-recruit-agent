# AI 智能招聘系统 — 项目技术文档

> 本文档系统性地记录了项目的架构设计、技术选型、核心模块实现细节与关键代码结构，
> 旨在为团队技术评审、后续迭代维护及工程实践沉淀提供完整的参考依据。

---

## 目录

- [一、技术栈总览](#一技术栈总览)
- [二、项目工程结构](#二项目工程结构)
- [三、数据库设计](#三数据库设计)
- [四、Agent 核心架构](#四agent-核心架构)
  - [4.1 对话编排核心：ConversationAgentService](#41-对话编排核心conversationagentservice)
  - [4.2 意图路由：IntentRouter](#42-意图路由intentrouter)
  - [4.3 动态锚点池：DynamicAnchorPool](#43-动态锚点池dynamicanchorpool)
  - [4.4 上下文组装：ContextAssembler](#44-上下文组装contextassembler)
  - [4.5 ReAct Agent：RecruitmentAgentService](#45-react-agentrecruitmentagentservice)
  - [4.6 Supervisor Agent：SupervisorAgentService](#46-supervisor-agentsupervisoragentservice)
  - [4.7 专家 Agent 工厂：SpecialistAgentFactory](#47-专家-agent-工厂specialistagentfactory)
  - [4.8 ReWOO 并行执行器：ReWooExecutor](#48-rewoo-并行执行器rewooexecutor)
- [五、记忆系统](#五记忆系统)
  - [5.1 短期记忆：RedisSessionMemory](#51-短期记忆redissessionmemory)
  - [5.2 长期记忆：PostgresLongTermMemory](#52-长期记忆postgreslongtermmemory)
  - [5.3 混合检索：HybridMemoryRetriever](#53-混合检索hybridmemoryretriever)
  - [5.4 自动提取：AutoMemoryExtractor](#54-自动提取automemoryextractor)
  - [5.5 记忆巩固：MemoryConsolidationAgent](#55-记忆巩固memoryconsolidationagent)
  - [5.6 记忆巩固调度：ConsolidationScheduler](#56-记忆巩固调度consolidationscheduler)
  - [5.7 遗忘机制一：MemoryDecayJob](#57-遗忘机制一memorydecayjob)
  - [5.8 遗忘机制二：MemoryForgettingService](#58-遗忘机制二memoryforgettingservice)
- [六、RAG 检索与候选人匹配](#六rag-检索与候选人匹配)
  - [6.1 Embedding 服务：EmbeddingService](#61-embedding-服务embeddingservice)
  - [6.2 Rerank 服务：RerankService](#62-rerank-服务rerankservice)
  - [6.3 向量搜索：VectorSearchService](#63-向量搜索vectorsearchservice)
  - [6.4 文档分块：DocumentChunkService](#64-文档分块documentchunkservice)
  - [6.5 候选人匹配：CandidateMatchService](#65-候选人匹配candidatematchservice)
  - [6.6 评估框架：EvaluationService](#66-评估框架evaluationservice)
  - [6.7 意图识别评估](#67-意图识别评估)
- [七、安全体系](#七安全体系)
  - [7.1 输入护栏：ConversationGuardrail](#71-输入护栏conversationguardrail)
  - [7.2 Reflexion 反思中间件：ReflexionMiddleware](#72-reflexion-反思中间件reflexionmiddleware)
  - [7.3 HITL 权限引擎：RecruitmentPermissionService](#73-hitl-权限引擎recruitmentpermissionservice)
  - [7.4 Sa-Token 认证授权](#74-sa-token-认证授权)
  - [7.5 PII 脱敏：AgentEventSseMapper](#75-pii-脱敏agenteventssemapper)
- [八、工具链路](#八工具链路)
  - [8.1 工具注册与调用](#81-工具注册与调用)
  - [8.2 通用简历搜索：ResumeSearchTool](#82-通用简历搜索resumesearchtool)
  - [8.3 岗位分析：JobAnalysisTool](#83-岗位分析jobanalysistool)
  - [8.4 候选人匹配：CandidateMatchingTool](#84-候选人匹配candidatematchingtool)
  - [8.5 面试出题：InterviewQuestionTool](#85-面试出题interviewquestiontool)
  - [8.6 AI 面试官：InterviewAgentTool](#86-ai-面试官interviewagenttool)
  - [8.7 候选人触达：OutreachAgentTool](#87-候选人触达outreachagenttool)
  - [8.8 联网搜索：WebSearchTool](#88-联网搜索websearchtool)
  - [8.9 Agent-as-Tool 包装器](#89-agent-as-tool-包装器)
- [九、LLM 服务层](#九llm-服务层)
  - [9.1 DeepSeekModelService](#91-deepseekmodelservice)
  - [9.2 LangFuseTraceService](#92-langfusetraceservice)
  - [9.3 JsonGuard](#93-jsonguard)
  - [9.4 QuickInfoExtractor](#94-quickinfoextractor)
  - [9.5 FileParserUtil](#95-fileparserutil)
- [十、事件系统与 SSE](#十事件系统与-sse)
  - [10.1 AgentEventPublisher](#101-agenteventpublisher)
  - [10.2 AgentEventSseMapper](#102-agenteventssemapper)
  - [10.3 AgentTraceAspect](#103-agenttraceaspect)
- [十一、异步任务系统](#十一异步任务系统)
  - [11.1 RabbitMQ 配置](#111-rabbitmq-配置)
  - [11.2 分析任务消费者](#112-分析任务消费者)
  - [11.3 任务状态管理](#113-任务状态管理)
- [十二、前端架构](#十二前端架构)
  - [12.1 技术选型与路由](#121-技术选型与路由)
  - [12.2 状态管理：auth store](#122-状态管理auth-store)
  - [12.3 SSE 流处理：useAgentStream](#123-sse-流处理useagentstream)
  - [12.4 核心组件](#124-核心组件)
  - [12.5 页面视图](#125-页面视图)
- [十三、配置与部署](#十三配置与部署)
  - [13.1 application.properties](#131-applicationproperties)
  - [13.2 agentscope.properties](#132-agentscopeproperties)
  - [13.3 LangFuse 可观测性](#133-langfuse-可观测性)
  - [13.4 Docker 部署](#134-docker-部署)
- [十四、API 接口清单](#十四api-接口清单)

---

## 一、技术栈总览

### 1.1 后端技术栈

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 语言 | Java | 17 | 主开发语言 |
| 框架 | Spring Boot | 3.4.1 | Web + AOP + AMQP + Actuator |
| 响应式 | Spring WebFlux | 3.4.1 | SSE 流式推送 |
| Agent 框架 | AgentScope | 2.0.0 | Agent 运行时（HarnessAgent、ReAct、中间件） |
| ORM | MyBatis-Plus | 3.0.4 | 数据库访问 |
| 数据库 | PostgreSQL | 16 | 主数据库 |
| 向量扩展 | pgvector | 0.8.5 | 向量存储与 HNSW 索引 |
| 全文检索 | pg_trgm | - | 关键词模糊搜索 |
| 缓存 | Redis (Lettuce) | 6.5.0 | 短期记忆 + Sa-Token 会话 |
| 消息队列 | RabbitMQ | - | 异步简历分析任务 |
| 认证 | Sa-Token | 1.39.0 | RBAC 权限控制 |
| 密码加密 | Spring Security Crypto | 6.4.2 | BCrypt（不引入完整 Spring Security） |
| JSON | Jackson | - | JSON 序列化/反序列化 |
| 文档解析 | Apache PDFBox / POI | 3.0.3 / 5.3.0 | PDF/DOC/DOCX 简历解析 |
| 监控 | Micrometer + Prometheus | - | 指标采集 |
| 日志 | Logback | - | 结构化日志 |

### 1.2 AI 能力

| 能力 | 提供方 | 模型 | 用途 |
|------|--------|------|------|
| LLM 对话 | 蚂蚁灵犀 (antchat.alipay.com) | deepseek-v4-flash | Agent 推理 + 意图分类 + 质量评估 |
| Embedding | 阿里云百炼 | text-embedding-v4 | 1024 维向量编码（简历、岗位、记忆、锚点） |
| Rerank | 阿里云百炼 | qwen3-rerank | 候选人交叉重排 |
| 联网搜索 | Tavily API | - | 实时信息补充（行业薪资、技术趋势） |
| 可观测性 | LangFuse (自托管) | 2.x | LLM 调用链路追踪 |

### 1.3 前端技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| 框架 | Vue 3 | 响应式 UI |
| UI 库 | Ant Design Vue | 企业级组件 |
| 构建工具 | Vite | 快速构建 |
| 状态管理 | Pinia (Composition API) | 认证状态 |
| HTTP | Axios | API 调用 |
| SSE | EventSource | Agent 流式响应 |

### 1.4 Maven 依赖配置

```xml
<!-- pom.xml 关键依赖 -->
<dependency>
    <groupId>io.agentscope</groupId>
    <artifactId>agentscope-harness</artifactId>
    <version>2.0.0</version>
</dependency>
<dependency>
    <groupId>io.agentscope</groupId>
    <artifactId>agentscope-extensions-model-openai</artifactId>
    <version>2.0.0</version>
</dependency>
<!-- AgentScope Maven 仓库 -->
<repository>
    <id>agentscope</id>
    <url>https://agentscope.gitee.io/maven-repo</url>
</repository>
```

编译配置开启 `-parameters` 以支持反射获取参数名。maven-surefire-plugin 使用 3.2.5 版本以支持 JUnit 5。

---

## 二、项目工程结构

```
demo/
├── src/main/java/com/example/recruit/
│   ├── DemoApplication.java                    # Spring Boot 启动类
│   ├── agent/
│   │   ├── core/                                # Agent 核心服务层
│   │   │   ├── ConversationAgentService.java   # 对话编排核心（523 行）
│   │   │   ├── RecruitmentAgentService.java    # ReAct Agent（261 行）
│   │   │   ├── SupervisorAgentService.java     # Supervisor Agent（115 行）
│   │   │   ├── SpecialistAgentFactory.java     # 专家 Agent 工厂（170 行）
│   │   │   └── ReWooExecutor.java              # ReWOO 并行执行器（178 行）
│   │   ├── routing/                             # 意图路由层
│   │   │   ├── IntentRouter.java               # 分层置信度意图识别（345 行）
│   │   │   ├── DynamicAnchorPool.java          # 动态锚点 LRU 管理
│   │   │   ├── EntityResolver.java             # 实体解析（"岗位1" → ID=1）
│   │   │   └── RecruitmentPermissionService.java # HITL 权限上下文
│   │   ├── context/                             # 上下文管理
│   │   │   ├── ContextAssembler.java           # 记忆+偏好注入 RuntimeContext
│   │   │   ├── SessionManager.java             # AgentScope 会话管理
│   │   │   ├── ChatSessionService.java         # 聊天会话 CRUD
│   │   │   └── ContextSnapshotService.java     # 上下文快照
│   │   ├── middleware/                          # Agent 中间件
│   │   │   ├── ConversationGuardrail.java      # 输入护栏（Injection + 偏见检测）
│   │   │   └── ReflexionMiddleware.java        # Reflexion 反思中间件
│   │   ├── event/                               # 事件系统
│   │   │   ├── AgentEventPublisher.java        # Agent 事件发布
│   │   │   ├── AgentEventSseMapper.java        # AgentEvent → SSE 映射 + PII 脱敏
│   │   │   └── AgentTraceAspect.java           # AOP 追踪切面
│   │   ├── tool/                                # Agent 工具（@Tool 注解）
│   │   │   ├── ResumeSearchTool.java            # 通用简历搜索
│   │   │   ├── JobAnalysisTool.java             # 岗位分析
│   │   │   ├── CandidateMatchingTool.java       # 候选人匹配
│   │   │   ├── InterviewQuestionTool.java       # 面试出题
│   │   │   ├── InterviewAgentTool.java          # AI 面试官
│   │   │   ├── OutreachAgentTool.java           # 候选人触达
│   │   │   ├── WebSearchTool.java               # 联网搜索
│   │   │   ├── ResumeAnalysisTool.java          # 简历分析
│   │   │   ├── JobAnalystAgentTool.java         # 岗位分析专家（Agent-as-Tool）
│   │   │   ├── MatchAgentTool.java              # 匹配专家（Agent-as-Tool）
│   │   │   ├── InterviewSpecialistTool.java     # 面试专家（Agent-as-Tool）
│   │   │   └── OutreachSpecialistTool.java      # 触达专家（Agent-as-Tool）
│   │   └── nudge/                               # 智能提醒
│   │       ├── PreferenceLearningService.java   # 偏好学习
│   │       └── ProactivePushService.java        # 主动推送
│   ├── config/                                  # 配置类
│   │   ├── AgentScopeConfig.java
│   │   ├── SaTokenConfig.java
│   │   ├── SecurityConfig.java
│   │   ├── WebConfig.java
│   │   ├── RabbitMQConfig.java
│   │   ├── MetricsConfig.java
│   │   ├── AuthExceptionHandler.java
│   │   └── ChunkBackfillRunner.java
│   ├── controller/                              # REST 控制器（14 个）
│   ├── service/                                 # 业务服务层
│   │   ├── analysis/                            # 简历分析结果模型
│   │   └── task/                                # 异步任务（RabbitMQ）
│   ├── memory/                                  # 记忆系统（8 个类）
│   ├── llm/                                     # LLM 服务层
│   ├── dal/                                     # 数据访问层
│   │   ├── entity/                              # 实体类（20 个）
│   │   ├── mapper/                              # MyBatis Mapper（20 个）
│   │   └── handler/                             # PostgreSQL 类型处理器
│   ├── dto/                                     # 数据传输对象
│   └── security/                                # 安全配置
├── src/main/resources/
│   ├── application.properties                   # 应用配置
│   ├── agentscope.properties                    # AgentScope 配置
│   ├── logback-spring.xml                       # 日志配置
│   ├── mapper/                                  # MyBatis XML 映射（20 个）
│   └── sql/                                     # 数据库 DDL
│       ├── schema.sql                           # 核心表（15 张表）
│       └── schema_auth.sql                      # 认证表（3 张表 + 种子数据）
├── frontend/                                    # Vue 3 前端
│   └── src/
│       ├── api/index.js                         # API 封装
│       ├── composables/                         # 组合式函数
│       ├── components/                          # 通用组件（12 个）
│       ├── views/                               # 页面视图（8 个）
│       ├── store/auth.js                        # Pinia 认证状态
│       └── router/index.js                      # Vue Router
├── experiments/                                 # 评估实验
│   └── intent-evaluation/                       # 意图识别评估
│       ├── dataset.json                         # 100 条标注数据
│       └── eval_report.json                     # 评估报告
└── docker-compose-langfuse.yml                  # LangFuse 自托管编排
```

**代码规模统计**：
- Java 后端：~130 个源文件，~70 个核心类
- 前端：28 个文件（8 视图 + 12 组件 + 3 composable + API + Router + Store）
- MyBatis XML 映射：20 个
- 数据库表：18 张（15 核心表 + 3 认证表）

---

## 三、数据库设计

### 3.1 核心表结构（schema.sql）

数据库使用 PostgreSQL 16 + pgvector 0.8.5 + pg_trgm 扩展。

```sql
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

#### 3.1.1 resume — 简历表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| candidate_name | VARCHAR(100) | 候选人姓名 |
| raw_text | TEXT | 原始简历文本 |
| parsed_json | JSONB | LLM 解析后的结构化数据 |
| embedding | VECTOR(1024) | 简历全文向量 |
| risk_tags | TEXT[] | 风险标签数组 |
| status | VARCHAR(20) | pending/reviewed/rejected |
| created_at / updated_at | TIMESTAMP | 时间戳 |

索引：
- `idx_resume_status` — 状态查询
- `idx_resume_embedding` — HNSW 向量索引，使用 `vector_cosine_ops` 操作符

#### 3.1.2 job_profile — 岗位画像表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| title | VARCHAR(200) | 岗位标题 |
| jd_text | TEXT | JD 原文 |
| department / level / location | VARCHAR | 部门/级别/地点 |
| salary_min / salary_max | INT | 薪资范围 |
| experience_min / experience_max | INT | 经验年限要求 |
| education | VARCHAR(50) | 学历要求 |
| headcount | INT | 招聘人数 |
| category | VARCHAR(100) | 岗位类别 |
| weight_matrix | JSONB | 技能权重矩阵 |
| role_graph | JSONB | 角色图谱 |
| growth_path | JSONB | 成长路径 |
| embedding | VECTOR(1024) | 岗位向量 |
| status | VARCHAR(20) | draft/active/closed |

#### 3.1.3 candidate_match — 候选人匹配表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| job_id | BIGINT FK | 关联岗位 |
| resume_id | BIGINT FK | 关联简历 |
| overall_score | DECIMAL(5,2) | 总分 |
| skill_score | DECIMAL(5,2) | 技能维度分 |
| experience_score | DECIMAL(5,2) | 经验维度分 |
| soft_score | DECIMAL(5,2) | 软素质维度分 |
| match_details | JSONB | 匹配详情（LLM 评分理由） |
| hr_feedback | TEXT | HR 反馈 |

#### 3.1.4 memory_entry — 长期记忆表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| agent_id | VARCHAR(64) | Agent ID（格式 `hr:{userId}`） |
| memory_key | VARCHAR(255) | 记忆键（snake_case） |
| memory_value | TEXT | 记忆值 |
| category | VARCHAR(50) | preference/fact/note/archived |
| access_count | INTEGER | 访问次数 |
| last_access | TIMESTAMP | 最后访问时间 |
| importance | DOUBLE PRECISION | 重要性分数 0.0-1.0 |
| embedding | VECTOR(1024) | 记忆向量 |
| created_at / updated_at | TIMESTAMP | 时间戳 |

约束：`UNIQUE(agent_id, memory_key)` — 同一 Agent 下记忆键唯一

#### 3.1.5 memory_graph — 记忆图谱边表

| 字段 | 类型 | 说明 |
|------|------|------|
| source_entry_id | BIGINT FK | 源记忆 ID |
| target_entry_id | BIGINT FK | 目标记忆 ID |
| agent_id | VARCHAR(64) | Agent ID（隔离） |
| relation_type | VARCHAR(50) | 关系类型（默认 `related_to`） |
| weight | DOUBLE PRECISION | 边权重 |

#### 3.1.6 document_chunk — 文档语义分块表

| 字段 | 类型 | 说明 |
|------|------|------|
| parent_type | VARCHAR(20) | 父类型（resume/job） |
| parent_id | BIGINT | 父 ID |
| chunk_index | INT | 分块序号 |
| chunk_type | VARCHAR(50) | 分块类型（skill/experience/education/summary） |
| content | TEXT | 分块文本 |
| embedding | VECTOR(1024) | 分块向量 |

约束：`UNIQUE(parent_type, parent_id, chunk_index)`

#### 3.1.7 其他核心表

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| interview | 面试记录 | job_id, resume_id, round, status, interviewer, scheduled_at |
| question | 面试题 | interview_id, type, content, follow_ups(JSONB), hr_adopted |
| interview_session | AI 面试对话 | interview_id, messages(JSONB), current_round, difficulty_level |
| interview_report | AI 面试报告 | interview_id, overall_score, tech/comm/problem_solving/culture_fit_score, strengths(TEXT[]), risks(TEXT[]), hiring_suggestion |
| interview_evaluation | 面试评估 | interview_id, tech/project/comm/learning/culture_score, tags(TEXT[]) |
| outreach | 候选人触达 | job_id, resume_id, message, status, batch_id |
| hr_preference | HR 偏好 | hr_id PK, preference_json(JSONB), expire_at |
| agent_trace | Agent 追踪 | session_id, agent_name, step_no, step_type, tool_name, input/output_text, model, tokens, latency_ms |
| consolidation_task | 记忆巩固任务队列 | status, entry_ids(BIGINT[]), result(JSONB) |
| evaluation_golden_sample | 评估金标样本 | category, input_text, expected_output, criteria_json |
| evaluation_result | 评估结果 | sample_id, actual_output, score, details_json |

### 3.2 认证表（schema_auth.sql）

| 表名 | 说明 |
|------|------|
| sys_user | 用户表（username, password BCrypt, real_name, email, phone, department, status） |
| sys_role | 角色表（code: HR/OPS, name, description） |
| sys_user_role | 用户角色关联表（多对多） |

种子数据：
- 角色：HR（招聘负责人）、OPS（运营人员）
- 用户：hr_user / ops_user（密码 123456，BCrypt 哈希）

### 3.3 向量索引策略

所有含 `embedding` 字段的表均使用 HNSW 索引：
```sql
CREATE INDEX idx_xxx_embedding ON xxx USING hnsw (embedding vector_cosine_ops);
```
HNSW 相比 IVFFlat 不需要显式训练聚类中心，适合增量插入场景。

---

## 四、Agent 核心架构

### 4.1 对话编排核心：ConversationAgentService

**文件**：`agent/core/ConversationAgentService.java`（523 行）

这是整个 Agent 系统的编排中枢，负责从用户消息到 SSE 响应的完整数据流。

**核心依赖注入**（构造器注入 14 个 Bean）：

```java
public ConversationAgentService(
    RecruitmentAgentService recruitmentAgentService,  // ReAct Agent
    SupervisorAgentService supervisorAgentService,    // Supervisor Agent
    ContextAssembler contextAssembler,                // 上下文组装
    SessionManager sessionManager,                    // 会话管理
    AgentEventSseMapper sseMapper,                    // SSE 映射
    ConsolidationScheduler consolidationScheduler,    // 记忆巩固调度
    LangFuseTraceService langFuseTraceService,        // LangFuse 追踪
    ChatSessionService chatSessionService,            // 聊天会话
    AutoMemoryExtractor autoMemoryExtractor,          // 自动记忆提取
    IntentRouter intentRouter,                        // 意图路由
    DeepSeekModelService deepSeekModelService,        // LLM 服务
    ReWooExecutor reWooExecutor,                      // ReWOO 执行器
    RedisSessionMemory redisSessionMemory,            // Redis 短期记忆
    AgentTraceService agentTraceService               // Agent 追踪
)
```

**核心方法 `stream(agentId, conversationId, userMessage)`**：

返回 `Flux<String>` — SSE 事件流。完整处理流程：

```
1. 记录开始时间 turnStartMs
2. 构建 Redis key: "agent:session:{agentId}"
3. 将用户消息追加到 Redis 会话历史
4. 调用 IntentRouter.classify(userMessage) 获取意图
5. 根据 IntentType 分流:
   - CHITCHAT → streamChichat()：取最近 3 条历史，调 DeepSeek.chat() 直答
   - BATCH_INDEPENDENT → streamBatch()：调 ReWooExecutor.execute() 并行执行
   - HITL → formatHitl()：生成人工确认事件，零 LLM 调用
   - SINGLE_TOOL → streamReAct()：走 RecruitmentAgentService.getHarnessAgent().streamEvents()
   - COMPOSITE → streamSupervisor()：走 SupervisorAgentService.getSupervisorAgent().streamEvents()
6. doOnComplete 异步后处理:
   - 将 assistant 回复追加到 Redis
   - 调用 AutoMemoryExtractor.extract() 提取记忆
   - 调用 ConsolidationScheduler 触发巩固检查
   - 记录 AgentTrace 到数据库
   - 清理 HybridMemoryRetriever 线程缓存
   - 推送 stats SSE 事件（tokens, latency, toolCalls）
```

**分流方法详解**：

- **`streamChichat()`**：不走 Agent 框架，直接调 `deepSeekModelService.chatStream()`，取最近 3 条历史作为上下文，1 次 LLM 调用。
- **`streamBatch()`**：调 `reWooExecutor.execute(userMessage)`，结果包装为 SSE text 事件。
- **`streamReAct()` / `streamSupervisor()`**：共用 `streamAgentEvents()` 方法，调 `agent.streamEvents()` 获取 AgentEvent 流，通过 `sseMapper.toSse()` 转换为 SSE 字符串。在流处理中跟踪 thinking 时间、工具调用次数、token 统计。
- **`finalizeTurn()`**：异步执行后处理逻辑——保存消息到数据库、触发记忆提取、推送统计事件。
- **`triggerEvaluationLoop()`**：查询本轮 AgentTrace，对关键工具输出做质量评估。

### 4.2 意图路由：IntentRouter

**文件**：`agent/routing/IntentRouter.java`（345 行）

采用**分层置信度 + 动态锚点自学习 + Top-2 LLM 验证**三段架构。

**常量定义**：

```java
private static final double HIGH_THRESHOLD = 0.85;        // 高置信度直接返回
private static final double MID_THRESHOLD = 0.65;          // 中置信度走 Top-2 验证
private static final double DYNAMIC_WRITEBACK_CONFIDENCE = 0.7; // LLM 结果回写阈值
private static final double SEMANTIC_DEDUP_THRESHOLD = 0.95;    // 语义去重阈值
```

**五类意图锚点**（静态预定义）：

```java
Map<IntentType, List<String>> anchorSentences = Map.of(
    IntentType.CHITCHAT, List.of("你好", "您好", "你是谁", "你能做什么", "谢谢", "再见",
                                 "介绍一下你自己", "今天天气怎么样", "帮我什么忙"),
    IntentType.SINGLE_TOOL, List.of("分析岗位1", "匹配候选人", "生成面试题", "查看简历3",
                                     "搜索Java最新趋势", "分析岗位的技能要求", "查看岗位列表"),
    IntentType.COMPOSITE, List.of("帮我招一个前端工程师", "全流程招聘P7后端",
                                   "从筛选到面试Java候选人", "帮我找一个会React的前端",
                                   "帮我完成整个招聘流程"),
    IntentType.HITL, List.of("发offer给张三", "淘汰这批候选人", "批量邀约5个候选人",
                              "确认入职", "拒绝候选人"),
    IntentType.BATCH_INDEPENDENT, List.of("分析岗位1、2、3并分别匹配候选人",
                                           "解析这些简历并生成面试题", "分别分析这三个岗位")
);
```

**初始化流程 `@PostConstruct initAnchors()`**：

```
1. 遍历 5 类锚点句
2. 对每句调用 EmbeddingService.embed() 获取 1024 维向量
3. 存入 anchorEmbeddings Map
4. 加载 DynamicAnchorPool 持久化文件（data/intent-anchors.json）
5. 为动态锚点补算 embedding（如果文件中未存储向量）
```

**分类逻辑 `classify(userMessage)`**：

```java
public Intent classify(String userMessage) {
    EmbeddingMatchResult match = matchWithEmbedding(userMessage);

    if (match.bestScore >= HIGH_THRESHOLD) {
        // 高置信度：直接返回，零 LLM 调用
        return new Intent(match.bestType, match.bestScore);
    }

    if (match.bestScore >= MID_THRESHOLD) {
        // 中置信度：Top-2 LLM 二选一验证
        Intent llmIntent = classifyWithLlmTop2(userMessage, match.bestType, match.secondType);
        maybeWriteback(llmIntent, userMessage);
        return llmIntent;
    }

    // 低置信度：LLM 全量五分类
    Intent llmIntent = classifyWithLlm(userMessage);
    maybeWriteback(llmIntent, userMessage);
    return llmIntent;
}
```

**`matchWithEmbedding(userMessage)`**：

```
1. 对用户输入调用 EmbeddingService.embed() 获取向量
2. 遍历所有静态锚点 + 动态锚点的 embedding
3. 计算余弦相似度，记录最高分(bestType/bestScore)和不同意图类型的次高分(secondType/secondScore)
4. 返回 EmbeddingMatchResult(bestType, bestScore, secondType, secondScore, userEmbedding)
```

关键设计：`secondType` 是从**不同意图类型**中选最高分，确保 Top-2 是两个不同意图的二选一。

**`classifyWithLlmTop2(userMessage, candidate1, candidate2)`**：

```java
private static final String TOP2_PROMPT_TEMPLATE = """
    你是意图分类器。用户输入可能是以下两种意图之一，请选择更匹配的一个：
    - %s: %s
    - %s: %s
    请以JSON输出: {"type":"%s|%s","confidence":0.0-1.0}
    """;
```

仅发送 2 个候选意图给 LLM 做二选一，prompt 约 60 tokens，比五分类的 ~200 tokens 短 70%。

**`classifyWithLlm(userMessage)`**：全量五分类，发送完整的 `CLASSIFY_PROMPT`，约 200 tokens。

**`maybeWriteback(intent, userMessage)`**：

```java
private void maybeWriteback(Intent intent, String userMessage) {
    if (intent.confidence >= DYNAMIC_WRITEBACK_CONFIDENCE) {
        float[] emb = embeddingService.embed(userMessage);
        dynamicAnchorPool.add(intent.type, userMessage, emb);
    }
}
```

LLM 分类结果 confidence >= 0.7 时，将用户输入回写为该意图类型的新锚点。

**余弦相似度计算**：

```java
private double cosineSimilarity(float[] a, float[] b) {
    double dotProduct = 0, normA = 0, normB = 0;
    for (int i = 0; i < a.length; i++) {
        dotProduct += a[i] * b[i];
        normA += a[i] * a[i];
        normB += b[i] * b[i];
    }
    return dotProduct / (Math.sqrt(normA) * Math.sqrt(normB));
}
```

**`@PreDestroy destroy()`**：销毁时 flush 动态锚点到磁盘。

### 4.3 动态锚点池：DynamicAnchorPool

**文件**：`agent/routing/DynamicAnchorPool.java`

管理 `Map<IntentType, LinkedHashSet<AnchorEntry>>`，每类意图最多 200 条动态锚点。

**核心字段**：

```java
private static final int MAX_PER_TYPE = 200;
private static final int FLUSH_INTERVAL = 50;  // 每 50 次写入 flush 一次
private final Map<IntentType, LinkedHashSet<AnchorEntry>> pool;
private int writeCount = 0;
```

**AnchorEntry 结构**：

```java
static class AnchorEntry {
    String text;
    float[] embedding;
    long createdAt;
    long lastHitTime;
}
```

**`add(type, text, embedding)`** 方法逻辑：

```
1. 语义去重：遍历该类型的已有锚点，如果与新文本的 cosine > 0.95，跳过
2. 添加到 LinkedHashSet（LRU 顺序）
3. 如果超过 MAX_PER_TYPE，移除最旧的（LinkedHashSet 的 iterator 顺序即为插入顺序）
4. writeCount++，每 FLUSH_INTERVAL 次触发异步 flush
```

**持久化**：

`flush()` 方法将池序列化为 JSON 写入 `data/intent-anchors.json`。使用 `new Thread(this::flush, "anchor-flush").start()` 异步写入（Java 17 不支持虚拟线程）。

`load()` 方法在启动时从 JSON 文件加载历史锚点。如果文件中没有存储 embedding，则补算。

### 4.4 上下文组装：ContextAssembler

**文件**：`agent/context/ContextAssembler.java`（90 行）

构建 enriched `RuntimeContext`，将短期记忆 + 长期记忆 + 偏好注入 Agent 上下文。

**`assemble(sessionId, userMessage, agentId)`** 方法：

```
1. 通过 SessionManager.getOrCreate(sessionId) 获取/创建 RuntimeContext
2. 注入 HR 偏好记忆：
   - 查询 memory_entry 中 category='preference' 的记忆
   - 按 importance 降序排序，取 Top 5
   - 格式化为 "§ {key}: {value}" 拼接
3. 调用 HybridMemoryRetriever.retrieve(agentId, userMessage) 做混合检索
   - 取 Top 5 结果，格式化拼接
4. 去重：用 Set<String> seenKeys 确保偏好和检索结果不重复
5. 注入到 RuntimeContext：
   ctx.put("memorySnapshot", "<memory>\n{memoryBlock}\n</memory>\n以上为历史记忆，如与当前指令冲突，以当前指令为准。")
```

异常处理：记忆检索失败不影响主流程，catch 后 log.warn 继续执行。

### 4.5 ReAct Agent：RecruitmentAgentService

**文件**：`agent/core/RecruitmentAgentService.java`（261 行）

封装 AgentScope `HarnessAgent`，注册 8 个业务工具，配置中间件和安全策略。

**`@PostConstruct init()`** 初始化流程：

```java
// 1. 手动构造 Model 对象（绕过 AgentScope 环境变量检查）
Model primaryModel = OpenAIChatModel.builder()
    .apiKey(apiKey)
    .baseUrl(baseUrl)
    .modelName("deepseek-v4-flash")
    .build();
Model fallbackModel = OpenAIChatModel.builder()
    .apiKey(apiKey)
    .baseUrl(baseUrl)
    .modelName("deepseek-v4-flash")
    .build();

// 2. 注册 8 个业务工具
Toolkit toolkit = new Toolkit();
toolkit.registerTool(interviewAgentTool);      // AI 面试官
toolkit.registerTool(jobAnalysisTool);          // 岗位分析
toolkit.registerTool(candidateMatchingTool);    // 候选人匹配
toolkit.registerTool(interviewQuestionTool);    // 面试出题
toolkit.registerTool(resumeAnalysisTool);       // 简历分析
toolkit.registerTool(resumeSearchTool);         // 通用简历搜索
toolkit.registerTool(outreachAgentTool);        // 候选人触达
toolkit.registerTool(webSearchTool);            // 联网搜索

// 3. 构建 HarnessAgent
this.harnessAgent = HarnessAgent.builder()
    .name("RecruitmentAgent")
    .sysPrompt("""
        你是AI招聘助手。根据HR指令调用工具完成招聘全流程。
        ## 工具调用链路
        1. 岗位：用 listJobs 查询岗位列表获取 job_id
        2. 简历：用 searchResumes 按姓名/学校/专业等条件搜索获取 resume_id
        3. 匹配：用 matchCandidates(job_id) 执行匹配，内部自动生成面试记录
        4. 面试题：用 generateQuestions(interview_id) 生成题目
        5. 面试：用 startInterview(interview_id) 启动AI初面
        6. 触达：用 generateOutreach(job_id, resume_id) 生成邀约
        ## 行为原则
        - 缺少ID时先用搜索工具查找，不要直接说"做不到"
        - 用户提到姓名/学校等条件时，先 searchResumes 获取 resume_id
        - 工具返回 error 时，根据提示调整参数重试，不要放弃
        """)
    .model(primaryModel)
    .fallbackModel(fallbackModel)
    .maxRetries(3)
    .toolkit(toolkit)
    .memory(MemoryConfig.defaults())
    .permissionContext(permissionService.getContext())
    .maxIters(3)
    .compaction(CompactionConfig.builder().triggerMessages(20).build())
    .middleware(conversationGuardrail)   // 输入护栏
    .middleware(reflexionMiddleware)     // Reflexion 反思
    .disableFilesystemTools()            // 禁用 6 类危险能力
    .disableShellTool()
    .disableSubagents()
    .disableDynamicSkills()
    .disableDefaultWorkspaceSkills()
    .disableWorkspaceContext()
    .disableMemoryTools()
    .build();
```

**关键配置项**：
- `maxIters(3)` — ReAct 最多迭代 3 次
- `maxRetries(3)` — LLM 调用失败最多重试 3 次
- `compaction(CompactionConfig.builder().triggerMessages(20).build())` — 20 条消息后触发上下文压缩
- `disable*()` — 禁用文件系统、Shell、子 Agent、动态技能、默认工作区、记忆工具

**sysPrompt 设计要点**：
- 明确工具调用链路（1→2→3→4→5→6），引导 LLM 按依赖顺序调用
- "缺少ID时先用搜索工具查找" — 解决 Agent 不主动探索的问题
- "工具返回 error 时调整参数重试" — 增强容错性

### 4.6 Supervisor Agent：SupervisorAgentService

**文件**：`agent/core/SupervisorAgentService.java`（115 行）

Supervisor Agent 本身不直接操作业务工具，而是将 4 个专家 Agent 作为工具调用（Agent-as-Tool 模式）。

**工具注册**：

```java
Toolkit toolkit = new Toolkit();
toolkit.registerTool(jobAnalystAgentTool);        // 岗位分析专家
toolkit.registerTool(matchAgentTool);             // 候选人匹配专家
toolkit.registerTool(interviewSpecialistTool);    // 面试专家
toolkit.registerTool(outreachSpecialistTool);     // 触达专家
toolkit.registerTool(webSearchTool);              // 联网搜索
```

**配置差异**（与 ReAct Agent 对比）：
- `maxIters(5)` — Supervisor 需要多轮调度，迭代上限更高
- Toolkit 中注册的是 Agent-as-Tool 包装器（JobAnalystAgentTool 等），而非底层业务工具
- sysPrompt 包含专家调用顺序和上下文传递策略

**sysPrompt 核心内容**：

```
你是招聘协调员。根据HR的需求，调度专家Agent完成全流程招聘。
## 专家Agent及调用顺序
1. jobAnalyst — 分析JD、提取技能矩阵
2. matchAgent — 匹配候选人
3. interviewSpecialist — 生成面试题或启动AI初面
4. outreachSpecialist — 生成邀约消息
## 工作原则
- 按招聘流程顺序调度：岗位分析 → 候选人匹配 → 面试/触达
- 将前一个专家的输出结果作为上下文写入下一个专家的instruction
- 用户只需单一环节时直接调用对应专家，无需全流程
- 信息不足时主动探索，而非直接拒绝
```

### 4.7 专家 Agent 工厂：SpecialistAgentFactory

**文件**：`agent/core/SpecialistAgentFactory.java`（170 行）

为 Supervisor 的 4 个 Agent-as-Tool 包装器分别构建独立的 `HarnessAgent` 实例。

**4 个专家 Agent 配置**：

| 专家 | 名称 | Toolkit | sysPrompt 要点 |
|------|------|---------|---------------|
| 岗位分析专家 | JobAnalystAgent | JobAnalysisTool | 先 listJobs 获取 ID，再 analyzeJob |
| 候选人匹配专家 | MatchAgent | CandidateMatchingTool + ResumeSearchTool | 先 searchResumes 获取简历 ID，再 matchCandidates |
| 面试专家 | InterviewSpecialistAgent | InterviewQuestionTool + InterviewAgentTool | 面试 ID 来自 matchCandidates 输出 |
| 触达专家 | OutreachSpecialistAgent | OutreachAgentTool + ResumeSearchTool | 简历 ID 可通过 searchResumes 搜索 |

**`buildSpecialist()` 方法**：

```java
private HarnessAgent buildSpecialist(String name, String sysPrompt, Toolkit toolkit,
                                      Model primary, Model fallback,
                                      RecruitmentPermissionService perm,
                                      ConversationGuardrail guardrail,
                                      ReflexionMiddleware reflexion) {
    return HarnessAgent.builder()
        .name(name)
        .sysPrompt(sysPrompt)
        .model(primary)
        .fallbackModel(fallback)
        .maxRetries(2)
        .toolkit(toolkit)
        .memory(MemoryConfig.defaults())
        .permissionContext(perm.getContext())
        .maxIters(3)
        .middleware(guardrail)
        .middleware(reflexion)
        .disableFilesystemTools()
        .disableShellTool()
        .disableSubagents()
        .disableDynamicSkills()
        .disableDefaultWorkspaceSkills()
        .disableWorkspaceContext()
        .disableMemoryTools()
        .build();
}
```

每个专家 Agent 拥有独立的 Toolkit（只注册该领域工具，避免工具误选）和独立的 sysPrompt。

### 4.8 ReWOO 并行执行器：ReWooExecutor

**文件**：`agent/core/ReWooExecutor.java`（178 行）

ReWOO（Reasoning WithOut Observation）三阶段批量执行器。

**线程池**：`ExecutorService executor = Executors.newFixedThreadPool(4)` — 4 线程并行执行。

**`execute(userMessage)`** 方法流程：

```
Phase 1: 规划 — 调用 LLM 将用户输入分解为独立子任务列表
    Prompt: "你是批量任务规划器。请将用户输入分解为独立的子任务列表..."
    输出: {"tasks":[{"tool":"JobAnalysisTool","args":{"jobId":1},"description":"分析岗位1"}]}
    解析为 List<BatchTask>

Phase 2: 并行执行 — CompletableFuture 并行调用工具
    for each BatchTask:
        CompletableFuture.supplyAsync(() -> executeTask(task), executor)
    CompletableFuture.allOf(...).join() 等待全部完成

Phase 3: 汇总 — 调用 LLM 合并多个工具执行结果
    Prompt: "你是结果汇总器。请将以下多个工具的执行结果合并为一份连贯的报告。"
```

**`executeTask(task)`**：根据 task.tool 名称路由到对应的 RecruitmentAgentService 方法（analyzeJob, matchCandidates, generateQuestions, analyzeResume）。

**数据结构**：

```java
private record BatchTask(String tool, JsonNode args, String description) {}
private record BatchResult(String tool, String description, String result) {}
```

**效果**：传统 ReAct 处理 N 个独立任务需要 N 次迭代（每次 Thought + Action + Observation），ReWOO 仅需 2 次 LLM 调用（Plan + Aggregate），中间工具调用 4 线程并行执行。

---

## 五、记忆系统

### 5.1 短期记忆：RedisSessionMemory

**文件**：`memory/RedisSessionMemory.java`（138 行）

**核心常量**：

```java
private static final String KEY_PREFIX = "agent:session:";
private static final long DEFAULT_TTL_SECONDS = 3600;    // 1 小时过期
private static final int MAX_HISTORY = 10;                // 最多 10 条消息
private static final int COMPRESS_THRESHOLD = 8;          // 8 条触发压缩
private static final int KEEP_RECENT_ON_COMPRESS = 4;     // 压缩时保留最近 4 条
```

**渐进式压缩 `compressHistory(sessionId, currentSize)`**：

当历史消息超过 `COMPRESS_THRESHOLD`(8) 时触发：
```
1. 读取全部历史消息
2. 将前面的 (total - KEEP_RECENT_ON_COMPRESS) 条消息拼接
3. 调用 DeepSeek.chatFast() 生成摘要（V4-Flash，快速模型）
4. 用 "[summary] {摘要}" 替换旧消息
5. 保留最近 4 条 + 摘要，写回 Redis
```

**Redis 数据结构**：使用 Redis List（LPUSH/RPUSH），每条消息 JSON 序列化存储为 `{role, content, timestamp}`。

### 5.2 长期记忆：PostgresLongTermMemory

**文件**：`memory/PostgresLongTermMemory.java`

封装 `MemoryEntryMapper` 的数据库操作。

**核心方法**：

| 方法 | 功能 |
|------|------|
| `save(agentId, key, value, category)` | 插入或更新记忆（UNIQUE 约束去重） |
| `searchByVector(agentId, queryVector, topK)` | pgvector 向量检索，`ORDER BY embedding <=> ?::vector LIMIT ?` |
| `searchByKeyword(agentId, keyword)` | pg_trgm 关键词模糊匹配，`WHERE memory_value ILIKE '%keyword%'` |
| `getByCategory(agentId, category)` | 按类别查询（preference/fact/note） |
| `getAll(agentId)` | 查询某 Agent 的全部记忆 |
| `updateImportance(id, importance)` | 更新重要性分数 |

### 5.3 混合检索：HybridMemoryRetriever

**文件**：`memory/HybridMemoryRetriever.java`（276 行）

三路混合检索 + RRF 融合 + 时间衰减 × 重要性加权。

**核心常量**：

```java
private static final int TOP_K = 10;
private static final double RECENCY_HALF_LIFE_DAYS = 30.0;  // 30 天半衰期
```

**`retrieve(agentId, query)`** 完整流程：

```
1. ThreadLocal 缓存检查：cacheKey = agentId + "|" + query
   → 命中则直接返回（避免 ReAct 迭代中重复 embedding API 调用）

2. 向量检索 vectorSearch(agentId, query):
   a. EmbeddingService.embed(query) 获取查询向量
   b. SQL: SELECT * FROM memory_entry WHERE agent_id = ? ORDER BY embedding <=> ?::vector LIMIT 10
   c. 返回 List<ScoredMemory>（rawScore = cosine similarity）

3. 关键词检索 keywordSearch(agentId, query):
   a. SQL: SELECT * FROM memory_entry WHERE agent_id = ? AND memory_value ILIKE '%query%' LIMIT 10
   b. 返回 List<ScoredMemory>（rawScore = 1.0）

4. 图谱游走 graphWalk(agentId, vectorResults, keywordResults):
   a. 从向量+关键词结果的 memory ID 集合作为 seed
   b. SQL: SELECT target_entry_id FROM memory_graph WHERE source_entry_id IN (seeds) AND agent_id = ?
   c. 反向查询：SELECT source_entry_id FROM memory_graph WHERE target_entry_id IN (seeds)
   d. 加载关联记忆，rawScore = edge weight

5. RRF 融合 rrfFusion(vectorResults, keywordResults, graphResults):
   公式: score = Σ 1/(k + rank_i)，k=60
   不依赖原始分数量纲，只用排名

6. 时间衰减 recencyFactor(entry):
   days = (now - entry.updated_at) 天数
   factor = exp(-days / RECENCY_HALF_LIFE_DAYS)
   → 30 天半衰期，模拟艾宾浩斯遗忘曲线

7. 重要性加权 importanceFactor(entry):
   importance >= 0.7 → factor = 1.5（加成）
   importance <= 0.3 → factor = 0.5（惩罚）
   否则 → factor = 1.0

8. 最终排序: finalScore = rrfScore * recencyFactor * importanceFactor
   按 finalScore 降序，取 Top 10

9. 写入 ThreadLocal 缓存
```

**RRF 融合实现**：

```java
private Map<Long, ScoredMemory> rrfFusion(List<ScoredMemory>... resultLists) {
    int k = 60;
    Map<Long, ScoredMemory> merged = new HashMap<>();
    for (List<ScoredMemory> list : resultLists) {
        for (int rank = 0; rank < list.size(); rank++) {
            ScoredMemory sm = list.get(rank);
            ScoredMemory existing = merged.get(sm.entry.getId());
            if (existing == null) {
                sm.rrfScore = 1.0 / (k + rank + 1);
                merged.put(sm.entry.getId(), sm);
            } else {
                existing.rrfScore += 1.0 / (k + rank + 1);
            }
        }
    }
    return merged;
}
```

**`clearCache()`**：静态方法，在每轮对话结束时调用，清理 ThreadLocal 缓存。

### 5.4 自动提取：AutoMemoryExtractor

**文件**：`memory/AutoMemoryExtractor.java`（110 行）

**触发关键词预过滤**：

```java
private static final Set<String> TRIGGER_KEYWORDS = Set.of(
    "偏好", "喜欢", "希望", "要求", "需要", "不要", "必须",
    "我是", "我叫", "我负责", "我们公司", "我们团队",
    "记住", "以后都", "默认", "经验", "技术栈",
    "纠正", "不是", "应该是", "重新"
);
```

**`extract(agentId, userMessage, assistantReply)`** 流程：

```
1. 短消息跳过：userMessage.length() < 5 → return
2. 关键词预过滤：hasTriggerKeyword(userMessage) == false → return
3. 输入截断：userMessage 和 assistantReply 各截取前 500 字符
4. 调用 V4-Flash LLM 提取：
   Prompt: "从对话中提取值得长期记住的信息。只提取：用户身份/角色、技术偏好、经验要求、明确纠正。"
   输出 JSON: {"memories":[{"key":"snake_case_key","value":"简短描述","category":"preference|fact|note"}]}
5. 解析 JSON，遍历 memories 数组
6. 对每条记忆调用 longTermMemory.save(agentId, key, value, category)
   → UNIQUE(agent_id, memory_key) 约束自动去重
```

### 5.5 记忆巩固：MemoryConsolidationAgent

**文件**：`memory/MemoryConsolidationAgent.java`（190 行）

LLM 驱动的 7 步记忆巩固（Hebb Mind 风格）。

**`@Transactional consolidate(entries, taskId, agentId)`** 流程：

```
1. 构造 Prompt：将待巩固记忆条目列表拼接为文本
2. 调用 V4-Flash 执行 7 步巩固：
   Step 1: 分类 → category (preference/fact/note)
   Step 2: 冲突解决 → 同 key 不同 value，保留最新或合并
   Step 3: 标签提取 → tags JSON 数组
   Step 4: 图谱边 → source_key → target_key, relation_type
   Step 5: 合并重复 → 语义相似度 > 0.95 的合并
   Step 6: 重要性评分 → 0.0-1.0（HR 显式偏好 = 0.8+, 自动提取 = 0.5）
   Step 7: 摘要 → 压缩描述

3. 解析 LLM 输出的 JSON:
   {"entries":[{"key":"...","category":"...","tags":[...],"importance":0.8,"summary":"...","value":"..."}],
    "edges":[{"source_key":"...","target_key":"...","relation_type":"...","weight":0.8}]}

4. 遍历 entries，更新 memory_entry 表：
   - 更新 category, importance, memory_value（用摘要替换）
   - 如果有 embedding，更新向量

5. 遍历 edges，插入 memory_graph 表：
   insertGraphEdge(sourceId, targetId, relationType, weight, agentId)

6. 更新 consolidation_task 状态为 completed
```

### 5.6 记忆巩固调度：ConsolidationScheduler

**文件**：`memory/ConsolidationScheduler.java`

**双触发机制**：

1. **定时触发**：`@Scheduled(cron = "0 0 * * * *")` — 每小时整点检查
2. **会话结束触发**：`triggerCheck()` — 在 ConversationAgentService 的 doOnComplete 中调用

**触发条件**：待巩固记忆 `>= 10` 条时才执行 LLM 巩固（避免少量记忆浪费 token）。

### 5.7 遗忘机制一：MemoryDecayJob

**文件**：`memory/MemoryDecayJob.java`（66 行）

**定时任务**：`@Scheduled(cron = "0 30 3 * * *")` — 每天凌晨 3:30 执行。

**核心常量**：

```java
private static final double DAILY_DECAY_FACTOR = 0.95;    // 每日衰减因子
private static final double ARCHIVE_THRESHOLD = 0.05;      // 归档阈值
private static final int CUTOFF_DAYS = 30;                  // 30 天前的记忆才衰减
```

**`runDailyDecay()`** 流程：

```
1. 查询所有 distinct agent_id（hr:1, hr:2, ...）确保按 HR 隔离
2. 对每个 agentId:
   a. memoryEntryMapper.applyDecay(agentId, 0.95, cutoffDate)
      SQL: UPDATE memory_entry SET importance = importance * 0.95
           WHERE agent_id = ? AND importance < 0.7 AND updated_at < ?
      → importance >= 0.7 的高价值记忆豁免
   b. memoryEntryMapper.archiveLowImportance(agentId, 0.05)
      SQL: UPDATE memory_entry SET category = 'archived'
           WHERE agent_id = ? AND importance < 0.05
```

### 5.8 遗忘机制二：MemoryForgettingService

**文件**：`memory/MemoryForgettingService.java`（80 行）

**定时任务**：`@Scheduled(cron = "0 30 * * * *")` — 每小时 30 分执行。

**核心常量**：

```java
private static final double DECAY_FACTOR = 0.8;           // 衰减因子
private static final int DECAY_CUTOFF_DAYS = 14;           // 14 天未访问
private static final double ARCHIVE_THRESHOLD = 0.15;      // 归档阈值
private static final int MAX_PER_USER = 200;               // 每用户最多 200 条
```

**`forget(agentId)`** 三步流程：

```
Step 1: 衰减 — 14 天未访问的记忆 importance *= 0.8（跳过 importance >= 0.7）
Step 2: 归档 — importance < 0.15 的记忆 category = 'archived'
Step 3: 容量限制 — 活跃记忆 > 200 条时，按 importance 升序删除最低的
        SQL: DELETE FROM memory_entry WHERE id IN (
            SELECT id FROM memory_entry WHERE agent_id = ?
            ORDER BY COALESCE(importance, 0.5) ASC, updated_at ASC LIMIT ?
        )
```

**双重遗忘对比**：

| 维度 | MemoryDecayJob | MemoryForgettingService |
|------|---------------|------------------------|
| 频率 | 每天 3:30 | 每小时 30 分 |
| 衰减因子 | 0.95（日级） | 0.8（时级） |
| 触发条件 | 30 天前更新 | 14 天未访问 |
| 豁免 | importance >= 0.7 | importance >= 0.7 |
| 归档阈值 | 0.05 | 0.15 |
| 容量限制 | 无 | 200 条/用户 |
| LLM 调用 | 0 | 0 |

---

## 六、RAG 检索与候选人匹配

### 6.1 Embedding 服务：EmbeddingService

**文件**：`llm/EmbeddingService.java`（68 行）

调用阿里云百炼 `text-embedding-v4` 模型，生成 1024 维向量。

**`embed(text)` 方法**：

```java
public float[] embed(String text) {
    ObjectNode requestBody = objectMapper.createObjectNode();
    requestBody.put("model", embeddingModel);       // "text-embedding-v4"
    requestBody.put("input", text);
    requestBody.put("dimension", dimension);        // 1024
    requestBody.put("encoding_format", "float");

    String response = webClient.post()
        .uri("/embeddings")
        .bodyValue(requestBody)
        .retrieve()
        .bodyToMono(String.class)
        .timeout(Duration.ofSeconds(10))
        .block();

    JsonNode embeddingNode = objectMapper.readTree(response).path("data").get(0).path("embedding");
    float[] result = new float[dimension];
    for (int i = 0; i < dimension && i < embeddingNode.size(); i++) {
        result[i] = (float) embeddingNode.get(i).asDouble();
    }
    return result;
}
```

WebClient 配置：
- baseUrl: `https://dashscope.aliyuncs.com/compatible-mode/v1`
- Header: `Authorization: Bearer {api-key}`
- 超时：10 秒

### 6.2 Rerank 服务：RerankService

**文件**：`llm/RerankService.java`（101 行）

调用阿里云百炼 `qwen3-rerank` 模型做交叉重排。

**`rerank(query, documents, topN)` 方法**：

```java
public List<Integer> rerank(String query, List<String> documents, int topN) {
    // 构造请求
    ObjectNode requestBody = MAPPER.createObjectNode();
    requestBody.put("model", model);           // "qwen3-rerank"
    requestBody.put("query", query);
    ArrayNode docsNode = requestBody.putArray("documents");
    documents.forEach(docsNode::add);

    // 调用 API
    String response = webClient.post()
        .uri("/rerank")
        .bodyValue(requestBody)
        .retrieve()
        .bodyToMono(String.class)
        .timeout(Duration.ofSeconds(15))
        .block();

    // 解析结果 — 返回排序后的文档索引列表
    JsonNode results = MAPPER.readTree(response).path("results");
    List<Integer> indices = new ArrayList<>();
    results.forEach(r -> indices.add(r.path("index").asInt()));
    return indices.size() > topN ? indices.subList(0, topN) : indices;
}
```

**容错**：API 调用失败时静默降级，返回 `IntStream.range(0, documents.size()).boxed().toList()`（原始顺序）。

### 6.3 向量搜索：VectorSearchService

**文件**：`service/VectorSearchService.java`

封装 pgvector 向量检索逻辑。

**核心方法**：

| 方法 | 功能 |
|------|------|
| `searchResumes(queryVector, topK, positionFilter)` | 简历向量搜索 + 方向预过滤 |
| `searchJobs(queryVector, topK)` | 岗位向量搜索 |
| `searchChunks(queryVector, parentType, topK)` | 文档分块向量搜索 |

**方向预过滤**：

```sql
-- 有方向过滤时
SELECT * FROM resume
WHERE embedding <=> ?::vector(1024)  -- 向量相似度排序
  AND (parsed_json->>'intended_position' ILIKE '%Java%'
       OR parsed_json->>'intended_position' ILIKE '%后端%')
ORDER BY embedding <=> ?::vector(1024)
LIMIT 20;

-- 无方向过滤时
SELECT * FROM resume
ORDER BY embedding <=> ?::vector(1024)
LIMIT 20;
```

### 6.4 文档分块：DocumentChunkService

**文件**：`service/DocumentChunkService.java`

简历和岗位的语义分块存储，支持父子索引（parent_type + parent_id）。

**分块策略**：

| chunk_type | 内容 | 来源 |
|------------|------|------|
| skill | 技能标签 | parsed_json → skills |
| experience | 工作经历 | parsed_json → work_experience |
| education | 教育背景 | parsed_json → education |
| summary | 摘要 | parsed_json → summary 或 raw_text 前 500 字符 |

**`chunkAndStore(parentType, parentId, parsedJson)`**：

```
1. 从 parsedJson 提取各字段
2. 为每个 chunk 生成文本内容
3. 调用 EmbeddingService.embed() 生成向量
4. 批量插入 document_chunk 表
```

**`ChunkBackfillRunner`**（配置类）：启动时检查并补全已有简历/岗位的分块数据。

### 6.5 候选人匹配：CandidateMatchService

**文件**：`service/CandidateMatchService.java`（397 行）

四阶段匹配全链路。

**`matchForJob(jobId)`** 完整流程：

```
Stage 1: pgvector Top20 召回 + 方向预过滤
  a. 查询 JobProfile 获取 title 和 requirements
  b. extractPositionFilters(jobTitle) → 提取方向关键词
     例: "Java高级开发工程师" → ["Java", "后端", "开发"]
  c. VectorSearchService.searchResumes(jobEmbedding, 20, filters)
  d. 如果有方向过滤结果为空，回退到无过滤搜索

Stage 2: 条件性 rerank 精排
  a. 统计候选池大小
  b. 候选池 <= 10：调用 RerankService.rerank() 精排
     - 构建候选人摘要（从 structuredData 提取自然语言文本）
     - qwen3-rerank 交叉重排
  c. 候选池 > 10：跳过 rerank，直接用向量检索顺序
     → 原因：大候选池中同类方向候选人技能重叠度高，
        文本 rerank 容易被无关细节误导

Stage 3: LLM 三维评分
  a. 对 Top5 候选人逐一调用 DeepSeek.chatJson() 评分
  b. 评分维度：
     - skill_score (0-100): 技能匹配度
     - experience_score (0-100): 经验匹配度
     - soft_score (0-100): 软素质评估
  c. 加载 HR 偏好作为软约束上下文

Stage 4: 透明加权排序
  finalScore = skill_score * 0.4 + experience_score * 0.3 + soft_score * 0.2 + vectorScore * 0.1
  按 finalScore 降序排列
  插入 candidate_match 表
```

**`extractPositionFilters(jobTitle)`**：

```java
private List<String> extractPositionFilters(String jobTitle) {
    List<String> filters = new ArrayList<>();
    String lower = jobTitle.toLowerCase();
    // 技术方向匹配
    if (lower.contains("java")) filters.add("Java");
    if (lower.contains("python")) filters.add("Python");
    if (lower.contains("前端") || lower.contains("frontend") || lower.contains("vue") || lower.contains("react"))
        filters.add("前端");
    if (lower.contains("后端") || lower.contains("backend")) filters.add("后端");
    if (lower.contains("全栈") || lower.contains("fullstack")) filters.add("全栈");
    if (lower.contains("算法") || lower.contains("ml") || lower.contains("ai")) filters.add("算法");
    if (lower.contains("运维") || lower.contains("devops")) filters.add("运维");
    if (lower.contains("测试") || lower.contains("qa")) filters.add("测试");
    if (lower.contains("产品") || lower.contains("pm")) filters.add("产品");
    // ...更多方向
    return filters;
}
```

**`buildCandidateSummary(resume)`**：

从 `parsed_json`（structuredData）提取自然语言文本，而非使用原始 JSON：

```java
private String buildCandidateSummary(Resume resume) {
    JsonNode data = resume.getParsedJson();
    StringBuilder sb = new StringBuilder();
    // 姓名
    sb.append("候选人: ").append(data.path("name").asText());
    // 意向岗位
    sb.append("，意向: ").append(data.path("intended_position").asText());
    // 工作年限
    sb.append("，经验: ").append(data.path("work_years").asText()).append("年");
    // 技能列表
    JsonNode skills = data.path("skills");
    if (skills.isArray()) {
        sb.append("，技能: ");
        for (int i = 0; i < Math.min(skills.size(), 10); i++) {
            sb.append(skills.get(i).asText()).append(" ");
        }
    }
    // 最近工作
    JsonNode experiences = data.path("work_experience");
    if (experiences.isArray() && !experiences.isEmpty()) {
        sb.append("，最近: ").append(experiences.get(0).path("company").asText());
        sb.append("-").append(experiences.get(0).path("position").asText());
    }
    return sb.toString();
}
```

### 6.6 评估框架：EvaluationService

**文件**：`service/EvaluationService.java`

五策略 × 五指标 × 三档 K 值的 RAG 评估框架。

**五策略**：

1. `in_memory` — 内存匹配（baseline）
2. `pgvector` — 纯向量检索
3. `pgvector+rerank` — 向量 + 重排
4. `pgvector+filter` — 向量 + 方向预过滤
5. `pgvector+filter+rerank` — 向量 + 方向预过滤 + 条件性 rerank

**五项 IR 指标**：

| 指标 | 公式 | 说明 |
|------|------|------|
| Recall@K | relevant_hit / total_relevant | 召回率 |
| Precision@K | relevant_hit / K | 精确率 |
| NDCG@K | Σ(rel_i / log2(i+1)) / IDCG | 归一化折损累积增益（支持 0-3 级分级相关性） |
| MRR | 1 / rank_of_first_relevant | 平均倒数排名 |
| HitRate@K | 1 if any relevant in top-K else 0 | 命中率 |

**评估 K 值**：5 / 10 / 20 三档

**数据集**：30 个岗位 JD × 50 份简历（1500 个配对），覆盖 13 个技术方向，标注 0-3 级相关性。相关配对 216 个（14.4%），其中高度相关 83 个、相关 69 个、弱相关 64 个。

**评估结果**（Top-5 平均，`experiments/rag-evaluation/eval_results.json`）：

| 策略 | Recall@5 | Precision@5 | NDCG@5 | MRR | HitRate@5 |
|------|----------|-------------|--------|-----|-----------|
| in_memory（基线） | 0.6689 | 0.5467 | 0.8074 | 0.9500 | 1.0000 |
| pgvector | 0.6897 | 0.5733 | 0.8232 | 0.9667 | 1.0000 |
| pgvector+rerank | 0.6714 | 0.5467 | 0.7904 | 0.9667 | 1.0000 |
| **pgvector+filter** | **0.7463** | **0.7678** | **0.8452** | 0.9500 | 1.0000 |
| pgvector+filter+rerank | 0.7063 | 0.6000 | 0.8348 | 0.9667 | 1.0000 |

**关键发现**：

1. **方向预过滤（filter）是最有效的优化**——`pgvector+filter` 在 Recall、Precision、NDCG 三项指标上均领先，Precision@5 从 0.57 提升到 0.77（+34%），因为 `extractPositionFilters` 提取的技术方向关键词（Java/前端/后端等）有效排除了跨方向候选人。
2. **Rerank 反而劣化排序**——`pgvector+rerank` 的 NDCG 比 `pgvector` 低 1.3%（0.7904 vs 0.8232）。根因：rerank 的 `instruct` 参数用了英文，与中文 JD+简历场景不匹配；文档摘要仅从 structuredData 提取，丢失了 rawText 中的关键上下文。
3. **Filter + Rerank 组合优于纯 Rerank**——加入 filter 预过滤后再 rerank，NDCG 回升到 0.8348，说明 rerank 在更干净的候选池上表现更好。
4. **所有策略 HitRate@5=1.0 + MRR≥0.95**——系统在"找到最相关候选人"这个核心任务上可靠，问题出在精细化排序上。

**优化方向**：

| 优先级 | 方向 | 具体措施 |
|--------|------|---------|
| P0 | Rerank instruct | 改为中文："根据岗位需求，按技术技能匹配度和相关工作经验对候选人简历排序" |
| P0 | Rerank 文档摘要 | 加入 rawText 前 500 字，不只依赖 structuredData |
| P0 | Rerank top_n | 从 5 改为 10，让 rerank 有更大排序空间 |
| P1 | 分块策略 | Python/测试类简历的分块应保留技能段完整性 |
| P2 | 评估指标 | 增加 Recall@10 和 Recall@20，更贴合实际 HR 使用场景 |

> 详细的指标解读见 `experiments/rag-evaluation/metrics-explanation.md`。

---

### 6.7 意图识别评估

**实验目录**：`experiments/intent-evaluation/`

对 `IntentRouter` 的分层置信度架构进行离线评估，数据集为 100 条标注数据（5 类意图 × 20 条）。

**四种配置对比**：

| 配置 | 说明 |
|------|------|
| `baseline` | 原始阈值（HIGH=0.85, MID=0.65），中/低置信度走全量五分类 LLM |
| `optimized_with_top2` | 中置信度走 Top-2 LLM 二选一验证（实际生产配置） |
| `optimized_no_top2` | 提高锚点命中率，不走 Top-2，直接全量五分类 |
| `optimized_margin` | Top-2 margin 置信度策略 |

**评估结果**（`experiments/intent-evaluation/eval_report.json`）：

| 配置 | 准确率 | Embedding 命中率 | LLM 调用数 | Token 消耗 |
|------|--------|-----------------|-----------|------------|
| baseline | 91.0% | 37.0% | 63 | 12,634 |
| **optimized_with_top2** | **91.0%** | 44.0% | 56 | **6,608** |
| optimized_no_top2 | 84.0% | 90.0% | 10 | 2,019 |
| optimized_margin | 90.0% | 57.0% | 43 | 8,690 |

**各意图准确率**（optimized_with_top2 配置）：

| 意图 | 准确率 |
|------|--------|
| CHITCHAT | 100.0% (20/20) |
| SINGLE_TOOL | 90.0% (18/20) |
| COMPOSITE | 80.0% (16/20) |
| HITL | 100.0% (20/20) |
| BATCH_INDEPENDENT | 85.0% (17/20) |

**关键发现**：

1. **Top-2 验证策略在保持准确率的同时节省 47.7% token**——`optimized_with_top2` 与 baseline 准确率相同（91.0%），但 token 从 12,634 降至 6,608，因为 Top-2 二选一 prompt 仅 ~60 tokens，比五分类的 ~200 tokens 短 70%。
2. **过度依赖 Embedding 会降低准确率**——`optimized_no_top2` 将 embedding 命中率提到 90%（只有 10 次 LLM 调用），但准确率降到 84.0%，COMPOSITE 从 85% 降到 70%，说明中置信度区域必须走 LLM 验证。
3. **COMPOSITE 是最难分类的意图**——所有配置下 COMPOSITE 准确率都最低（70%-85%），因为"帮我招一个前端工程师"这类复合意图与 SINGLE_TOOL 的边界模糊。
4. **HITL 意图识别最稳定**——Top-2 配置下达到 100%，因为"发offer""淘汰候选人"等锚点句与业务强绑定，语义区分度高。

---

## 七、安全体系

### 7.1 输入护栏：ConversationGuardrail

**文件**：`agent/middleware/ConversationGuardrail.java`（95 行）

实现 `MiddlewareBase` 接口的 `onAgent()` 方法，在 Agent 处理前拦截恶意输入。

**正则规则**：

```java
// Prompt Injection 检测
private static final Pattern PROMPT_INJECTION = Pattern.compile(
    "(?i)(ignore|disregard|forget).*(previous|above|system).*(instruction|prompt|rule)|" +
    "(?i)you\\s+are\\s+(now|no\\s+longer)|" +
    "(?i)(reveal|show|print).*(system|hidden|secret).*(prompt|instruction)|" +
    "(?i)act\\s+as\\s+(if\\s+you\\s+have\\s+no|without).*(rule|restriction|guardrail)"
);

// 招聘偏见检测
private static final Pattern BIAS_INPUT = Pattern.compile(
    "(?i)(只|仅)(招|聘|要|推荐)(男|女|汉族|少数民族|本地|外地)|" +
    "(?i)(不要|拒|排除)(女|男|大龄|残障|已婚|未婚|怀孕)|" +
    "(?i)(只要|仅限)(985|211|全日制|统招)"
);
```

**拦截流程**：

```java
@Override
public Flux<AgentEvent> onAgent(Agent agent, RuntimeContext ctx, AgentInput input,
                                 Function<AgentInput, Flux<AgentEvent>> next) {
    String userText = extractUserText(input);
    if (userText != null) {
        GuardResult result = checkInput(userText);
        if (result.blocked()) {
            return Flux.just(new CustomEvent("guardrail_blocked", Map.of(
                "error", "消息被安全护栏拦截: " + result.reason(),
                "reason", result.reason()
            )));
        }
    }
    return next.apply(input);  // 放行
}
```

拦截时返回 `guardrail_blocked` 事件，不进入 Agent 执行。

### 7.2 Reflexion 反思中间件：ReflexionMiddleware

**文件**：`agent/middleware/ReflexionMiddleware.java`（130 行）

实现 `MiddlewareBase` 接口的 `onActing()` 方法，在工具执行后评估输出质量。

**核心常量**：

```java
private static final int MAX_RETRIES = 2;           // 最多重试 2 次
private static final double SCORE_THRESHOLD = 0.75;  // 质量阈值
private static final Set<String> CRITICAL_TOOLS = Set.of(
    "matchCandidates", "generateQuestions", "generateOutreach"
);
```

**评估 Prompt**：

```
你是质量评估器。请对以下工具输出按维度打分：
- 相关性 (0-1): 与任务需求的匹配度
- 完整性 (0-1): 信息是否全面
- 合规性 (0-1): 是否符合招聘规范
请以JSON输出: {"relevance":0.8,"completeness":0.7,"compliance":0.9,"overall":0.8,"issues":"问题描述"}
```

**`onActing()` 流程**：

```
1. 检查工具名是否在 CRITICAL_TOOLS 集合中
   → 只有 matchCandidates, generateQuestions, generateOutreach 触发评估
2. 工具执行后获取输出
3. 调用 V4-Flash 评估（非关键工具跳过）
4. 解析评估结果 JSON
5. 如果 overall < 0.75 且重试次数 < MAX_RETRIES:
   a. 构造反思 Hint：将 issues 作为反馈注入
   b. retryCount.incrementAndGet()
   c. 返回 HintBlockEvent 触发重试
6. 否则放行结果
```

**重试计数**：使用 `ConcurrentHashMap<String, Integer> retryCount` 按工具调用 ID 隔离。

### 7.3 HITL 权限引擎：RecruitmentPermissionService

**文件**：`agent/routing/RecruitmentPermissionService.java`

构建 AgentScope `PermissionContext`，定义工具执行权限。

**权限映射**（`security/PermissionMapping.java`）：

| 工具 | 角色 | 是否需要 HITL |
|------|------|--------------|
| matchCandidates | HR | 否 |
| generateQuestions | HR, OPS | 否 |
| generateOutreach | HR | 否 |
| startInterview | HR | 否 |
| analyzeJob | HR, OPS | 否 |
| searchResumes | HR, OPS | 否 |
| webSearch | HR, OPS | 否 |

### 7.4 Sa-Token 认证授权

**文件**：`config/SaTokenConfig.java` + `security/StpInterfaceImpl.java`

**配置**（application.properties）：

```properties
sa-token.token-name=satoken
sa-token.timeout=7200          # 2 小时过期
sa-token.is-concurrent=true     # 允许并发登录
sa-token.token-style=uuid
sa-token.is-read-header=true    # 从 Header 读取
sa-token.is-read-cookie=false
```

**StpInterfaceImpl** 实现 Sa-Token 的 `getPermissionList()` 和 `getRoleList()`：

```java
@Override
public List<String> getPermissionList(Object loginId, String loginType) {
    // 从 sys_user_role → sys_role 查询用户角色
    // 返回角色 code 列表（HR / OPS）
}

@Override
public List<String> getRoleList(Object loginId, String loginType) {
    // 同上，返回角色列表
}
```

**认证流程**（`AuthService`）：

```
1. 用户提交 username + password
2. 查询 sys_user 表验证用户名存在
3. BCrypt.matches(password, user.password) 验证密码
4. StpUtil.login(user.id) 创建 Sa-Token 会话
5. 返回 LoginResponse(token, userInfo)
```

**密码加密**：使用 Spring Security Crypto 的 `BCryptPasswordEncoder`，不引入完整 Spring Security 框架。

### 7.5 PII 脱敏：AgentEventSseMapper

**文件**：`agent/event/AgentEventSseMapper.java`（275 行）

在 SSE 输出层对 PII 信息进行脱敏。

**PII 正则**：

```java
private static final Pattern PII_PHONE = Pattern.compile("\\b1[3-9]\\d{9}\\b");        // 手机号
private static final Pattern PII_ID_CARD = Pattern.compile("\\b\\d{17}[0-9Xx]\\b");   // 身份证
private static final Pattern PII_EMAIL = Pattern.compile("\\b[\\w.-]+@[\\w.-]+\\.\\w{2,}\\b"); // 邮箱
```

**`maskPii(text)` 方法**：

```java
static String maskPii(String text) {
    if (text == null) return null;
    text = PII_PHONE.matcher(text).replaceAll(m -> {
        String s = m.group();
        return s.substring(0, 3) + "****" + s.substring(7);
    });
    text = PII_ID_CARD.matcher(text).replaceAll(m -> {
        String s = m.group();
        return s.substring(0, 6) + "********" + s.substring(14);
    });
    text = PII_EMAIL.matcher(text).replaceAll(m -> {
        String s = m.group();
        int at = s.indexOf('@');
        return s.charAt(0) + "***" + s.substring(at);
    });
    return text;
}
```

**为什么在 SSE 层脱敏**：AgentScope 的 `Msg` 对象是不可变的，无法在中间件中修改输出内容。SSE 映射层是输出到用户的最后一站，在此脱敏最可靠。

---

## 八、工具链路

### 8.1 工具注册与调用

所有工具类使用 AgentScope 的 `@Tool` 注解注册，通过 `Toolkit.registerTool()` 注册到 Agent。

**工具层次结构**：

```
                    ┌─ SINGLE_TOOL 路径 ────────────────┐
                    │                                     │
RecruitmentAgentService (8 个底层工具)                     │
  ├── ResumeSearchTool                                     │
  ├── JobAnalysisTool                                      │
  ├── CandidateMatchingTool                                │
  ├── InterviewQuestionTool                                │
  ├── InterviewAgentTool                                   │
  ├── OutreachAgentTool                                    │
  ├── ResumeAnalysisTool                                   │
  └── WebSearchTool                                        │
                    │                                      │
SupervisorAgentService (5 个 Agent-as-Tool)                │
  ├── JobAnalystAgentTool → JobAnalystAgent (内含 1 工具)   │
  ├── MatchAgentTool → MatchAgent (内含 2 工具)            │
  ├── InterviewSpecialistTool → InterviewSpecialist (2 工具)│
  ├── OutreachSpecialistTool → OutreachSpecialist (2 工具)  │
  └── WebSearchTool (共用)                                  │
```

### 8.2 通用简历搜索：ResumeSearchTool

**文件**：`agent/tool/ResumeSearchTool.java`

一个多参数可选的通用搜索工具，替代 N 个按字段逐个搜索的工具。

```java
@Tool(name = "searchResumes",
      description = "通用简历搜索：按姓名/学校/学历/专业/意向岗位/工作年限组合查询...")
public List<Map<String, Object>> searchResumes(
    @ToolParam(name = "name", description = "候选人姓名（可选）") String name,
    @ToolParam(name = "school", description = "毕业院校（可选）") String school,
    @ToolParam(name = "education", description = "学历（可选）") String education,
    @ToolParam(name = "major", description = "专业（可选）") String major,
    @ToolParam(name = "intendedPosition", description = "意向岗位（可选）") String intendedPosition,
    @ToolParam(name = "minExperience", description = "最低工作年限（可选）") Integer minExperience
)
```

所有参数可选，LLM 按需组合调用。底层使用 MyBatis 动态 SQL 构建 WHERE 条件。

### 8.3 岗位分析：JobAnalysisTool

**文件**：`agent/tool/JobAnalysisTool.java`

```java
@Tool(name = "listJobs", description = "查询岗位列表，返回所有岗位的ID和标题")
public List<Map<String, Object>> listJobs()

@Tool(name = "analyzeJob", description = "分析岗位JD，提取技能要求、权重矩阵、角色图谱")
public Map<String, Object> analyzeJob(@ToolParam(name = "jobId") Long jobId)
```

`analyzeJob` 内部调用 `JobAnalysisService`，使用 LLM 解析 JD 并生成结构化分析结果（weight_matrix, role_graph, growth_path）。

### 8.4 候选人匹配：CandidateMatchingTool

**文件**：`agent/tool/CandidateMatchingTool.java`

```java
@Tool(name = "matchCandidates", description = "为指定岗位匹配候选人，返回Top5候选人列表")
public Map<String, Object> matchCandidates(@ToolParam(name = "jobId") Long jobId)
```

底层调用 `CandidateMatchService.matchForJob(jobId)`，执行四阶段匹配全链路。

### 8.5 面试出题：InterviewQuestionTool

**文件**：`agent/tool/InterviewQuestionTool.java`

```java
@Tool(name = "generateQuestions", description = "为面试生成面试题")
public Map<String, Object> generateQuestions(@ToolParam(name = "interviewId") Long interviewId)

@Tool(name = "getQuestions", description = "查看已有面试题")
public List<Map<String, Object>> getQuestions(@ToolParam(name = "interviewId") Long interviewId)
```

### 8.6 AI 面试官：InterviewAgentTool

**文件**：`agent/tool/InterviewAgentTool.java`

```java
@Tool(name = "startInterview", description = "启动AI初面")
public Map<String, Object> startInterview(@ToolParam(name = "interviewId") Long interviewId)

@Tool(name = "evaluateAnswer", description = "评估面试回答")
public Map<String, Object> evaluateAnswer(...)

@Tool(name = "generateSummary", description = "生成面试总结报告")
public Map<String, Object> generateSummary(...)
```

**返回值截断**：面试对话 messages 可能上千字，工具返回时截断到 300-500 字，避免撑爆 ReAct 上下文。

### 8.7 候选人触达：OutreachAgentTool

**文件**：`agent/tool/OutreachAgentTool.java`

```java
@Tool(name = "generateOutreach", description = "为单个候选人生成邀约消息")
public Map<String, Object> generateOutreach(
    @ToolParam(name = "jobId") Long jobId,
    @ToolParam(name = "resumeId") Long resumeId
)

@Tool(name = "generateBatchOutreach", description = "批量生成邀约消息")
public Map<String, Object> generateBatchOutreach(
    @ToolParam(name = "jobId") Long jobId,
    @ToolParam(name = "resumeIds") String resumeIds  // 逗号分隔
)
```

### 8.8 联网搜索：WebSearchTool

**文件**：`agent/tool/WebSearchTool.java`

调用 Tavily API 做联网搜索，补充实时信息（行业薪资、技术趋势）。

```java
@Tool(name = "webSearch", description = "联网搜索实时信息")
public Map<String, Object> webSearch(@ToolParam(name = "query") String query)
```

底层调用 Tavily `/search` 端点，返回结果列表（title, url, content），引用时用 `[1] [2]` 标注来源。

### 8.9 Agent-as-Tool 包装器

4 个包装器将专家 Agent 包装为工具，供 Supervisor 调用：

| 包装器 | 底层专家 Agent | @Tool 方法 |
|--------|--------------|-----------|
| JobAnalystAgentTool | JobAnalystAgent | `jobAnalyst(instruction)` |
| MatchAgentTool | MatchAgent | `matchAgent(instruction)` |
| InterviewSpecialistTool | InterviewSpecialistAgent | `interviewSpecialist(instruction)` |
| OutreachSpecialistTool | OutreachSpecialistAgent | `outreachSpecialist(instruction)` |

**调用流程**：

```
Supervisor Agent 调用 @Tool 方法（传入 instruction 字符串）
  → 包装器调用底层专家 Agent 的 chat(instruction)
  → 专家 Agent 独立 ReAct 循环执行
  → 返回结果给 Supervisor
  → Supervisor 整合多个专家结果
```

---

## 九、LLM 服务层

### 9.1 DeepSeekModelService

**文件**：`llm/DeepSeekModelService.java`（269 行）

统一 LLM 调用入口，封装 WebClient 调用蚂蚁灵犀 OpenAI 兼容接口。

**API 方法**：

| 方法 | 模型 | 用途 | 返回 |
|------|------|------|------|
| `chat(prompt)` | primary (v4-flash) | 简单对话 | String |
| `chat(systemPrompt, userMessage)` | primary | 带系统提示的对话 | String |
| `chatFast(systemPrompt, userMessage)` | fast (v4-flash) | 快速推理 | String |
| `chatFastWithUsage(systemPrompt, userMessage)` | fast | 带 token 统计 | ChatResult(content, inputTokens, outputTokens) |
| `chatJson(systemPrompt, userMessage)` | primary | JSON 输出 | String |
| `chatWithTools(systemPrompt, userMessage, tools)` | primary | 带工具调用 | String |
| `chatStream(systemPrompt, userMessage)` | primary | 流式调用 | Flux<String> |

**WebClient 配置**：

```java
this.webClient = WebClient.builder()
    .baseUrl(baseUrl)      // https://antchat.alipay.com/v1
    .defaultHeader("Authorization", "Bearer " + apiKey)
    .defaultHeader("Content-Type", "application/json")
    .build();
```

**`doChat(model, systemPrompt, userMessage, stream)` 核心逻辑**：

```java
ObjectNode requestBody = objectMapper.createObjectNode();
requestBody.put("model", model.replace("openai:", ""));  // 去掉 "openai:" 前缀
requestBody.put("temperature", 0.7);
requestBody.put("max_tokens", 4096);

ArrayNode messages = requestBody.putArray("messages");
ObjectNode sysMsg = messages.addObject();
sysMsg.put("role", "system");
sysMsg.put("content", systemPrompt);
ObjectNode userMsg = messages.addObject();
userMsg.put("role", "user");
userMsg.put("content", userMessage);

String response = webClient.post()
    .uri("/chat/completions")
    .bodyValue(requestBody)
    .retrieve()
    .bodyToMono(String.class)
    .timeout(Duration.ofSeconds(60))
    .block();

return objectMapper.readTree(response)
    .path("choices").get(0)
    .path("message").path("content").asText();
```

**`doChatWithUsage`** — 带 429 重试逻辑：

```java
private ChatResult doChatWithUsage(String model, String systemPrompt, String userMessage) {
    int maxRetries = 3;
    for (int attempt = 0; attempt < maxRetries; attempt++) {
        try {
            // ... 调用 API ...
            int inputTokens = root.path("usage").path("prompt_tokens").asInt();
            int outputTokens = root.path("usage").path("completion_tokens").asInt();
            return new ChatResult(content, inputTokens, outputTokens);
        } catch (Exception e) {
            if (e.getMessage().contains("429") && attempt < maxRetries - 1) {
                Thread.sleep(2000);  // 429 限流重试
                continue;
            }
            throw e;
        }
    }
}
```

**`doChatStream`** — SSE 流式调用：

```java
private Flux<String> doChatStream(String model, String systemPrompt, String userMessage) {
    requestBody.put("stream", true);
    return webClient.post()
        .uri("/chat/completions")
        .bodyValue(requestBody)
        .retrieve()
        .bodyToFlux(String.class)      // 逐块接收
        .filter(chunk -> !chunk.equals("[DONE]"))
        .map(chunk -> {
            JsonNode node = objectMapper.readTree(chunk);
            return node.path("choices").get(0)
                .path("delta").path("content").asText("");
        })
        .filter(text -> !text.isEmpty());
}
```

**日志追踪**：`getCallerInfo()` 遍历调用栈，找到实际调用 `doChat` 的类名和方法名，用于日志关联。

### 9.2 LangFuseTraceService

**文件**：`llm/LangFuseTraceService.java`

可选的 LLM 调用链路追踪，通过 HTTP 发送到 LangFuse 自托管实例。

**配置**：

```properties
app.langfuse.enabled=false                    # 默认关闭
app.langfuse.base-url=http://localhost:3000
app.langfuse.public-key=
app.langfuse.secret-key=
```

**功能**：
- 记录每次 LLM 调用的 model、prompt、response、tokens、latency
- 发送到 LangFuse `/api/public/ingestion` 端点
- 在 LangFuse UI 中可视化 Agent 调用链路

### 9.3 JsonGuard

**文件**：`llm/JsonGuard.java`

JSON 解析防护工具，处理 LLM 输出中的 JSON 格式问题。

**`parseJsonSafe(text)` 方法**：

```java
public static JsonNode parseJsonSafe(String text) {
    // 1. 直接尝试解析
    try {
        return objectMapper.readTree(text);
    } catch (Exception ignored) {}

    // 2. 提取第一个 {...} 块
    int start = text.indexOf('{');
    int end = text.lastIndexOf('}');
    if (start >= 0 && end > start) {
        try {
            return objectMapper.readTree(text.substring(start, end + 1));
        } catch (Exception ignored) {}
    }

    // 3. 返回 null
    return null;
}
```

### 9.4 QuickInfoExtractor

**文件**：`llm/QuickInfoExtractor.java`

从简历 raw_text 中快速提取关键信息（姓名、电话、邮箱、技能列表），用于简历上传后的预填充。

### 9.5 FileParserUtil

**文件**：`llm/FileParserUtil.java`

简历文件解析工具，支持 PDF / DOC / DOCX 格式。

**配置**：

```properties
app.fileparser.python-command=/Users/lly/.local/pipx/venvs/markitdown-mcp/bin/python3
app.fileparser.script-path=scripts/parse_resume.py
```

使用 Python markitdown 库解析文件，通过 ProcessBuilder 调用 Python 脚本。

---

## 十、事件系统与 SSE

### 10.1 AgentEventPublisher

**文件**：`agent/event/AgentEventPublisher.java`

发布 Agent 运行事件到 Spring 事件总线，用于解耦的异步处理（如 trace 记录、统计分析）。

### 10.2 AgentEventSseMapper

**文件**：`agent/event/AgentEventSseMapper.java`（275 行）

将 AgentScope 的 28 种 `AgentEvent` 子类映射为 14 种 SSE 事件类型。

**SSE 事件类型**：

| 事件 | 数据 | 说明 |
|------|------|------|
| `session` | {sessionId, replyId} | 会话开始 |
| `plan` | {plan} | ReWOO 规划结果 |
| `task_update` | {task, status} | 批量任务状态更新 |
| `thinking` | {duration} | Agent 思考阶段 |
| `text` | {delta, isLast} | 文本增量推送 |
| `tool_call` | {name, args, progressHint} | 工具调用开始 |
| `tool_result` | {name, result} | 工具调用结果 |
| `hint` | {hint} | Reflexion 反思提示 |
| `data` | {type, data} | 结构化数据 |
| `hitl` | {replyId, message, userMessage} | 人工确认请求 |
| `push` | {message} | 主动推送通知 |
| `trace` | {model, tokens, latency} | LLM 调用追踪 |
| `stop` | {} | 停止 |
| `error` | {error} | 错误 |
| `done` | {} | 完成 |
| `stats` | {tokens, latency, toolCalls, searchCount} | 统计信息 |

**工具进度提示映射**：

```java
private static final Map<String, String> TOOL_PROGRESS_HINTS = Map.ofEntries(
    Map.entry("listJobs", "正在查询岗位列表..."),
    Map.entry("analyzeJob", "正在分析岗位详情..."),
    Map.entry("matchCandidates", "正在分析候选人匹配..."),
    Map.entry("generateQuestions", "正在生成面试题..."),
    Map.entry("webSearch", "正在联网搜索..."),
    Map.entry("generateOutreach", "正在生成邀约草稿..."),
    Map.entry("startInterview", "正在启动AI面试..."),
    Map.entry("jobAnalyst", "正在调用岗位分析专家..."),
    Map.entry("matchAgent", "正在调用候选人匹配专家..."),
    // ...
);
```

**`toSse(event)` 核心映射逻辑**：

```java
public String toSse(AgentEvent event) {
    if (event instanceof TextDeltaEvent e) {
        return format("text", mapOf("delta", e.getText(), "isLast", false));
    }
    if (event instanceof ToolCallEvent e) {
        String hint = getProgressHint(e.getToolName());
        return format("tool_call", mapOf("name", e.getToolName(), "args", e.getArgs(), "progressHint", hint));
    }
    if (event instanceof ToolResultEvent e) {
        String result = maskPii(e.getResult());  // PII 脱敏
        return format("tool_result", mapOf("name", e.getToolName(), "result", result));
    }
    if (event instanceof HintBlockEvent e) {
        return format("hint", mapOf("hint", e.getHint()));
    }
    if (event instanceof CustomEvent e) {
        if ("guardrail_blocked".equals(e.getName())) {
            return format("error", e.getData());
        }
        // ... 其他自定义事件
    }
    return null;  // 静默跳过
}
```

**SSE 格式**：

```java
private String format(String eventType, Map<String, Object> data) {
    return "event: " + eventType + "\n" +
           "data: " + objectMapper.writeValueAsString(data) + "\n\n";
}
```

### 10.3 AgentTraceAspect

**文件**：`agent/event/AgentTraceAspect.java`

AOP 切面，拦截 Agent 方法调用，记录到 `agent_trace` 表。

**记录内容**：

```java
AgentTrace trace = new AgentTrace();
trace.setSessionId(sessionId);
trace.setAgentName(agentName);
trace.setStepNo(stepNo);
trace.setStepType(stepType);      // thinking / tool_call / tool_result / text
trace.setToolName(toolName);
trace.setInputText(inputText);     // 截断到 2000 字符
trace.setOutputText(outputText);
trace.setModel(model);
trace.setTokens(tokens);
trace.setLatencyMs(latency);
trace.setStatus("success" / "error");
```

---

## 十一、异步任务系统

### 11.1 RabbitMQ 配置

**文件**：`config/RabbitMQConfig.java`

```java
public static final String ANALYSIS_EXCHANGE = "analysis.exchange";
public static final String ANALYSIS_QUEUE = "analysis.queue";
public static final String ANALYSIS_ROUTING_KEY = "analysis.task";

// DirectExchange + durable Queue + binding
@Bean public DirectExchange analysisExchange() { ... }
@Bean public Queue analysisQueue() { return QueueBuilder.durable(ANALYSIS_QUEUE).build(); }
@Bean public Binding analysisBinding() { ... }

// Jackson JSON 序列化
@Bean public MessageConverter jsonMessageConverter() {
    return new Jackson2JsonMessageConverter();
}
```

### 11.2 分析任务消费者

**文件**：`service/task/AnalysisTaskConsumer.java`

```java
@RabbitListener(queues = RabbitMQConfig.ANALYSIS_QUEUE)
public void handleAnalysisTask(AnalysisTaskMessage message) {
    // 1. 解析任务消息（taskId, type, params）
    // 2. 更新任务状态为 processing
    // 3. 执行分析（简历分析 / 岗位分析 / 批量匹配）
    // 4. 更新任务状态为 completed / failed
}
```

**AnalysisTaskMessage**：

```java
public class AnalysisTaskMessage {
    String taskId;
    String type;        // "resume_analysis" / "job_analysis" / "batch_match"
    Map<String, Object> params;
    String userId;
}
```

### 11.3 任务状态管理

**文件**：`service/task/TaskStatusManager.java` + `service/task/TaskStatus.java`

```java
public enum TaskStatus {
    PENDING, PROCESSING, COMPLETED, FAILED
}
```

`TaskStatusManager` 使用 Redis 存储任务状态，支持前端轮询查询进度。

**TaskStatusController** 提供 REST API：

```
GET /api/task/{taskId}/status  → 查询任务状态
```

---

## 十二、前端架构

### 12.1 技术选型与路由

**文件**：`frontend/src/router/index.js`

Vue 3 + Vue Router 4，8 个页面路由：

| 路径 | 组件 | 认证 | 说明 |
|------|------|------|------|
| `/login` | LoginView | 否 | 登录页 |
| `/` | DashboardView | 是 | 仪表盘首页 |
| `/chat` | AgentChatView | 是 | Agent 对话主界面 |
| `/jobs` | JobsView | 是 | 岗位管理 |
| `/resumes` | ResumesView | 是 | 简历管理 |
| `/matches` | MatchesView | 是 | 候选人匹配 |
| `/interviews` | InterviewsView | 是 | 面试管理 |
| `/interview-agent` | InterviewAgentView | 是 | AI 面试官 |

**路由守卫**：

```javascript
router.beforeEach((to, from, next) => {
    const authStore = useAuthStore()
    if (to.meta.requiresAuth && !authStore.isAuthenticated) {
        next('/login')
    } else {
        next()
    }
})
```

### 12.2 状态管理：auth store

**文件**：`frontend/src/store/auth.js`

使用 Pinia Composition API 风格：

```javascript
export const useAuthStore = defineStore('auth', () => {
    const token = ref(localStorage.getItem('token') || '')
    const user = ref(JSON.parse(localStorage.getItem('user') || 'null'))

    const isAuthenticated = computed(() => !!token.value)
    const role = computed(() => user.value?.role || '')

    function login(tokenValue, userInfo) {
        token.value = tokenValue
        user.value = userInfo
        localStorage.setItem('token', tokenValue)
        localStorage.setItem('user', JSON.stringify(userInfo))
    }

    function logout() {
        token.value = ''
        user.value = null
        localStorage.removeItem('token')
        localStorage.removeItem('user')
    }

    return { token, user, isAuthenticated, role, login, logout }
})
```

### 12.3 SSE 流处理：useAgentStream

**文件**：`frontend/src/composables/useAgentStream.js`（432 行）

核心组合式函数，处理 Agent SSE 事件流。

**响应式状态**：

```javascript
const messages = ref([])                 // 消息列表
const sending = ref(false)               // 发送中状态
const sessionId = ref('default')          // 当前会话 ID
const conversationId = ref(null)          // 对话 ID
const sessions = ref([])                  // 会话列表
const activeToolCalls = ref([])           // 当前工具调用
const currentPlan = ref(null)             // ReWOO 规划
const pendingHitl = ref(null)             // HITL 确认
const currentTrace = ref(null)            // 追踪数据
const pushMessages = ref([])              // 推送消息
const turnStats = ref(null)               // 统计
const currentSessionTokens = ref({})      // Token 统计
```

**`send(text)` 方法**：

```javascript
async function send(text) {
    sending.value = true
    pushUserMessage(text)
    const assistantMsg = pushAssistantPlaceholder()

    // 使用 fetch + ReadableStream 处理 SSE
    const response = await fetch('/api/agent/chat/stream', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'satoken': authStore.token
        },
        body: JSON.stringify({ message: text, sessionId: sessionId.value })
    })

    const reader = response.body.getReader()
    const decoder = new TextDecoder()
    let buffer = ''

    while (true) {
        const { done, value } = await reader.read()
        if (done) break
        buffer += decoder.decode(value, { stream: true })

        // 解析 SSE 帧（event: type\ndata: json\n\n）
        const frames = buffer.split('\n\n')
        buffer = frames.pop()  // 最后一个可能不完整

        for (const frame of frames) {
            const lines = frame.split('\n')
            const eventType = lines[0].replace('event: ', '')
            const data = JSON.parse(lines[1].replace('data: ', ''))
            handleEvent(eventType, data, assistantMsg)
        }
    }
    sending.value = false
}
```

**`handleEvent(eventType, data, assistantMsg)`** — 事件状态机：

```javascript
function handleEvent(eventType, data, assistantMsg) {
    switch (eventType) {
        case 'session':
            sessionId.value = data.sessionId
            break
        case 'thinking':
            assistantMsg.thinking = true
            break
        case 'text':
            assistantMsg.content += data.delta
            if (data.isLast) assistantMsg.thinking = false
            break
        case 'tool_call':
            handleToolCall(data, assistantMsg)
            break
        case 'tool_result':
            handleToolResult(data, assistantMsg)
            break
        case 'plan':
            currentPlan.value = data.plan
            break
        case 'hitl':
            pendingHitl.value = data
            break
        case 'trace':
            currentTrace.value = data
            break
        case 'push':
            pushMessages.value.push(data)
            break
        case 'stats':
            turnStats.value = data
            break
        case 'error':
            assistantMsg.content += `\n[错误] ${data.error}`
            assistantMsg.thinking = false
            break
        case 'done':
            assistantMsg.thinking = false
            break
    }
}
```

### 12.4 核心组件

| 组件 | 文件 | 功能 |
|------|------|------|
| ThinkingBox | `components/ThinkingBox.vue` | Agent 思考动画 |
| ToolBlock | `components/ToolBlock.vue` | 工具调用展示（展开/收起） |
| PlanTrack | `components/PlanTrack.vue` | ReWOO 规划进度追踪 |
| HitlCard | `components/HitlCard.vue` | HITL 人工确认卡片 |
| TracePanel | `components/TracePanel.vue` | LLM 调用追踪面板（model, tokens, latency） |
| BatchResultCard | `components/BatchResultCard.vue` | 批量任务结果卡片 |
| SessionSidebar | `components/SessionSidebar.vue` | 会话列表侧边栏 |
| PushMessage | `components/PushMessage.vue` | 主动推送通知 |
| SearchSources | `components/SearchSources.vue` | 联网搜索来源展示 |
| StatsBar | `components/StatsBar.vue` | 统计信息栏 |
| ResultCard | `components/ResultCard.vue` | 通用结果卡片 |

### 12.5 页面视图

**AgentChatView**（核心页面）：

- 使用 `useAgentStream` composable 管理 SSE 流
- 左侧 SessionSidebar 展示会话列表
- 右侧消息流区域：ThinkingBox → ToolBlock → text 交替展示
- 底部输入框 + 发送按钮 + 停止按钮
- 右侧可展开 TracePanel 和 StatsBar

**DashboardView**：

- 统计概览（简历数、岗位数、面试数、匹配数）
- 最近活动时间线

**MatchesView**：

- 岗位选择 → 候选人匹配结果列表
- 透明评分展示（skill/exp/soft/vector 各维度分数）
- HR 反馈输入

**InterviewAgentView**：

- AI 面试官对话界面
- 实时难度调整
- 面试报告生成

---

## 十三、配置与部署

### 13.1 application.properties

```properties
# ─── Server ───
server.port=8888

# ─── PostgreSQL ───
spring.datasource.url=jdbc:postgresql://localhost:5432/airecruit
spring.datasource.username=lly
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# ─── MyBatis ───
mybatis.mapper-locations=classpath*:mapper/*.xml
mybatis.type-handlers-package=com.example.recruit.dal.handler
mybatis.configuration.map-underscore-to-camel-case=true

# ─── Redis ───
spring.data.redis.host=localhost
spring.data.redis.port=6379

# ─── Actuator ───
management.endpoints.web.exposure.include=health,prometheus,metrics
management.endpoint.health.show-details=always

# ─── AI Model (蚂蚁灵犀 OpenAI 兼容端点) ───
app.ai.api-key={your-api-key}
app.ai.base-url=https://antchat.alipay.com/v1
app.ai.model.primary=openai:deepseek-v4-flash
app.ai.model.fast=openai:deepseek-v4-flash

# ─── Embedding (阿里云百炼) ───
app.embedding.api-key={your-api-key}
app.embedding.base-url=https://dashscope.aliyuncs.com/compatible-mode/v1
app.embedding.model=text-embedding-v4
app.embedding.dimension=1024

# ─── Rerank (阿里云百炼) ───
app.rerank.api-key={your-api-key}
app.rerank.base-url=https://dashscope.aliyuncs.com/compatible-api/v1
app.rerank.model=qwen3-rerank

# ─── File Upload ───
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ─── File Parser (markitdown) ───
app.fileparser.python-command={path-to-python3}
app.fileparser.script-path=scripts/parse_resume.py

# ─── RabbitMQ ───
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# ─── LangFuse (自托管，默认关闭) ───
app.langfuse.enabled=false
app.langfuse.base-url=http://localhost:3000

# ─── Web Search (Tavily) ───
app.web-search.api-key={your-tavily-key}
app.web-search.base-url=https://api.tavily.com

# ─── Sa-Token ───
sa-token.token-name=satoken
sa-token.timeout=7200
sa-token.is-concurrent=true
sa-token.token-style=uuid
sa-token.is-read-header=true
sa-token.is-read-cookie=false
```

### 13.2 agentscope.properties

```properties
# Default model for HarnessAgent
agentscope.model.default=openai:deepseek-v4-pro

# Logging
agentscope.logging.level=INFO
agentscope.logging.enable-trace=true

# Workspace
agentscope.workspace.type=local
agentscope.workspace.path=./workspace

# Memory
agentscope.memory.short-term.type=redis
agentscope.memory.short-term.ttl=3600
agentscope.memory.long-term.type=postgresql
```

### 13.3 LangFuse 可观测性

**Docker Compose 编排**（`docker-compose-langfuse.yml`）：

```yaml
services:
  langfuse-db:          # PostgreSQL 16 (端口 5433)
    image: postgres:16-alpine

  langfuse-clickhouse:  # ClickHouse (端口 8124)
    image: clickhouse/clickhouse-server:24.3-alpine

  langfuse:             # LangFuse 2.x (端口 3000)
    image: langfuse/langfuse:2
    depends_on:
      - langfuse-db
      - langfuse-clickhouse
    environment:
      DATABASE_URL: postgresql://langfuse:langfuse123@langfuse-db:5432/langfuse
      CLICKHOUSE_URL: http://langfuse-clickhouse:8123
      NEXTAUTH_URL: http://localhost:3000
```

**启动**：`docker-compose -f docker-compose-langfuse.yml up -d`

**集成**：设置 `app.langfuse.enabled=true` + 配置 public-key 和 secret-key，系统会自动将 LLM 调用发送到 LangFuse 追踪。

### 13.4 Docker 部署

**环境依赖**：

| 组件 | 版本 | 端口 |
|------|------|------|
| PostgreSQL | 16 + pgvector 0.8.5 + pg_trgm | 5432 |
| Redis | 7.x | 6379 |
| RabbitMQ | 3.x | 5672 |
| LangFuse (可选) | 2.x | 3000 |
| Java | 17 | - |

**数据库初始化**：

```bash
psql -U lly -d airecruit -f src/main/resources/sql/schema.sql
psql -U lly -d airecruit -f src/main/resources/sql/schema_auth.sql
```

**后端启动**：

```bash
mvn spring-boot:run
# 或打包后运行
mvn clean package -DskipTests
java -jar target/recruit-1.0.0.jar
```

**前端启动**：

```bash
cd frontend
npm install
npm run dev    # 开发模式
npm run build  # 生产构建
```

---

## 十四、API 接口清单

### 14.1 认证 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| POST | `/api/auth/login` | 否 | 用户登录 |
| POST | `/api/auth/logout` | 是 | 用户登出 |
| GET | `/api/auth/userinfo` | 是 | 获取当前用户信息 |

### 14.2 Agent 对话 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| POST | `/api/agent/chat/stream` | 是 | Agent SSE 流式对话（核心接口） |
| POST | `/api/agent/chat` | 是 | Agent 非流式对话 |
| POST | `/api/agent/stop` | 是 | 停止当前对话 |
| POST | `/api/agent/hitl/confirm` | 是 | HITL 人工确认 |

### 14.3 会话管理 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/chat/sessions` | 是 | 列出会话 |
| POST | `/api/chat/sessions` | 是 | 创建会话 |
| DELETE | `/api/chat/sessions/{id}` | 是 | 删除会话 |
| GET | `/api/chat/sessions/{id}/messages` | 是 | 获取会话消息 |
| GET | `/api/chat/sessions/{id}/tokens` | 是 | 获取会话 token 统计 |

### 14.4 岗位 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/jobs` | 是 | 岗位列表 |
| GET | `/api/jobs/{id}` | 是 | 岗位详情 |
| POST | `/api/jobs` | 是 | 创建岗位 |
| PUT | `/api/jobs/{id}` | 是 | 更新岗位 |
| DELETE | `/api/jobs/{id}` | 是 | 删除岗位 |
| POST | `/api/jobs/{id}/analyze` | 是 | LLM 分析岗位 |

### 14.5 简历 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/resumes` | 是 | 简历列表 |
| GET | `/api/resumes/{id}` | 是 | 简历详情 |
| POST | `/api/resumes/upload` | 是 | 上传简历文件 |
| POST | `/api/resumes/{id}/analyze` | 是 | LLM 分析简历 |
| GET | `/api/resumes/search` | 是 | 搜索简历 |

### 14.6 候选人匹配 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| POST | `/api/matches/{jobId}` | 是 | 执行匹配 |
| GET | `/api/matches/{jobId}` | 是 | 查看匹配结果 |
| GET | `/api/matches/{jobId}/sorted` | 是 | 排序查看匹配结果 |
| POST | `/api/matches/{id}/feedback` | 是 | HR 反馈 |

### 14.7 面试 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/interviews` | 是 | 面试列表 |
| POST | `/api/interviews` | 是 | 创建面试 |
| POST | `/api/interviews/{id}/questions` | 是 | 生成面试题 |
| GET | `/api/interviews/{id}/questions` | 是 | 查看面试题 |

### 14.8 AI 面试官 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| POST | `/api/interview-agent/{id}/start` | 是 | 启动 AI 初面 |
| POST | `/api/interview-agent/{id}/answer` | 是 | 提交面试回答 |
| POST | `/api/interview-agent/{id}/summary` | 是 | 生成面试总结 |

### 14.9 评估 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| POST | `/api/evaluation/run` | 是 | 执行 RAG 评估 |
| GET | `/api/evaluation/results` | 是 | 查看评估结果 |

### 14.10 仪表盘 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/dashboard/stats` | 是 | 获取统计概览 |

### 14.11 异步任务 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/task/{taskId}/status` | 是 | 查询任务状态 |

### 14.12 SSE 事件 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/events/{userId}` | 是 | SSE 事件订阅（推送通知） |

### 14.13 用户管理 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/users` | HR | 用户列表 |
| POST | `/api/users` | HR | 创建用户 |

### 14.14 健康检查 API

| Method | Path | 认证 | 说明 |
|--------|------|------|------|
| GET | `/api/health` | 否 | 健康检查 |
| GET | `/actuator/health` | 否 | Spring Boot Actuator 健康检查 |
| GET | `/actuator/prometheus` | 否 | Prometheus 指标 |

---

> **文档结束**。本文档涵盖了系统从数据库设计到前端交互的完整技术实现细节，各模块的类名、方法签名、关键算法、配置参数均与代码库保持一致。后续迭代时请同步更新对应章节。
