# 复刻项目(mianshiCopy)→ 原项目(AImianshi)实施清单（交付执行型 AI）

> **生成日期**:2026-07-28
> **权威蓝本**:`/Users/lly/Desktop/AImianshi/demo`（原项目，遇到任何不确定一律以这里同名文件为准）
> **改造目标**:`/Users/lly/Desktop/mianshiCopy/ai-recruit-agent/demo`（复刻项目）
> **目标**:让复刻项目**完全复刻**原项目的技术栈、分层架构、API 契约与实现。

---

## 给执行者的总原则（先读）

1. **逐文件移植法**:本文档对每个需新建/纠正的文件都给出"职责 + 方法清单 + 约束"。实现时**打开原项目同名文件作为权威蓝本**,逐方法对照实现。复刻已有的同名文件,先与原项目 `diff`,把缺失逻辑从原项目回填。
2. **保留决策(用户已确认)**:**ORM 保持 MyBatis-Plus,不换回原生 MyBatis**。但原项目由 Mapper XML 实现的复杂查询(向量检索、批量、统计、join)必须在 Plus 下**等价补回**(见 §4)。
3. **必须纠正的偏离**:① 数据库 H2 降级 → 回归 PostgreSQL+pgvector;② 删除 Java 端余弦相似度降级,改回 pgvector 检索;③ Service 逻辑已下沉进 Tool → 拆回 Service 层;④ Controller 路径/端点与原项目不一致 → 逐字对齐;⑤ 安全规则缩水 → 补全。
4. **不要新增原项目没有的功能、不要重命名包/类**(除非本文档明确要求)。复刻已有的 `AppProperties`/`MockChatModel`/`ChatResult` 等额外文件可保留但不作为复刻重点。
5. 每完成一个文件,对照本清单对应小节的"验收点"勾选。

---

# 第一部分:技术栈与配置基准（P0 阻断性,先做）

## §1 POM 依赖修订

| artifactId | 原项目 | 复刻现状 | 动作 |
|------------|--------|---------|------|
| `h2` | ❌ 无 | ✅ 有 | **删除**(回归 PostgreSQL) |
| `mybatis-spring-boot-starter` | ✅ 有 | — | 原项目用原生 MyBatis;**复刻保持 `mybatis-plus-spring-boot3-starter`,不改**(用户决策) |
| `mybatis-plus-spring-boot3-starter` | — | ✅ 有 | **保留** |
| `postgis-jdbc` | ✅ 有 | ❌ 缺 | **补** |
| `jackson-databind` | ✅ 显式 | ❌ 缺 | **补**(避免传递依赖漂移) |
| `spring-boot-dependencies`(BOM) | ✅ import | ❌(用 parent) | 建议对齐为 BOM,但 parent 方式可保留 |
| `poi`/`poi-scratchpad` | ❌(`poi-ooxml`) | ✅ 多 | 对齐原项目(只留 `poi-ooxml`) |
| `spring-boot-starter-validation` | ❌ | ✅ | 核对 DTO 是否用 `@Valid`,按需保留 |
| 其余(Spring Boot/AgentScope/Sa-Token/webflux/amqp/redis/actuator) | — | — | 已对齐 ✅ |

**验收**:POM 中无 `h2`;有 `postgis-jdbc`、`jackson-databind`;保留 `mybatis-plus-spring-boot3-starter`。

## §2 数据库回归 PostgreSQL + pgvector（删除所有 H2 降级）

**schema 规格要求**(以原项目 `resources/sql/schema.sql` 为准):
- `CREATE EXTENSION IF NOT EXISTS vector;` + `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
- 向量列统一 `VECTOR(1024)`:`resume.embedding`、`job_profile.embedding`、`memory_entry.embedding`
- 向量索引统一 HNSW + 余弦:`USING hnsw (embedding vector_cosine_ops)`
- 业务表:`resume / job_profile / candidate_match / interview / question / interview_evaluation / agent_trace / hr_preference / outreach / memory_entry / memory_graph / consolidation_task / interview_session / interview_report / evaluation_golden_sample / evaluation_result`(+ auth 库的 `sys_user / sys_role / sys_user_role`)
- 复刻 schema.sql 若已对齐则 `diff` 确认上述项齐全;`BIGINT[]` 数组列(如 `consolidation_task.entry_ids`)保留。

**代码层删除项**(关键):
- `CandidateMatchingTool` 注释与实现里"为兼容 H2/Mock 降级,向量召回改为 Java 端余弦相似度计算"——**删除该降级逻辑**,改回经 `VectorSearchService` 调 pgvector。
- 任何 `@Conditional` 里的 H2/Mock 分支判定,核对后移除。

**验收**:复刻无 H2 依赖、无 Java 端向量降级;能连真实 PostgreSQL+pgvector 跑通向量召回。

## §3 application.properties 配置

**先补实际 `application.properties`**(复刻目前只有 `.example`)。以下为原项目 `application.properties.example` 全文,**作为复刻配置蓝本**:

```properties
# Server
server.port=8888

# PostgreSQL DataSource
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/airecruit
spring.datasource.username=lly
spring.datasource.password=

# MyBatis —— 【复刻注意】保留 MyBatis-Plus 时,前缀须从 mybatis.* 改为 mybatis-plus.*
mybatis-plus.mapper-locations=classpath*:mapper/*.xml
mybatis-plus.type-handlers-package=com.example.recruit.dal.handler
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.configuration.use-generated-keys=true

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Actuator
management.endpoints.web.exposure.include=health,prometheus,metrics
management.endpoint.health.show-details=always

# AI Model (OpenAI 兼容端点, AgentScope 解析 openai:{model})
app.ai.api-key=<your-ai-api-key>
app.ai.base-url=https://antchat.alipay.com/v1
app.ai.model.primary=openai:deepseek-v4-flash
app.ai.model.fast=openai:deepseek-v4-flash

# 阿里云百炼 Embedding (text-embedding-v4, 维度 1024)
app.embedding.api-key=<your-dashscope-api-key>
app.embedding.base-url=https://dashscope.aliyuncs.com/compatible-mode/v1
app.embedding.model=text-embedding-v4
app.embedding.dimension=1024

# 阿里云百炼 Rerank (qwen3-rerank)
app.rerank.api-key=<your-dashscope-api-key>
app.rerank.base-url=https://dashscope.aliyuncs.com/compatible-api/v1
app.rerank.model=qwen3-rerank

# File Upload & Parser
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.fileparser.python-command=<path-to-python3>
app.fileparser.script-path=scripts/parse_resume.py

# RabbitMQ
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# LangFuse
app.langfuse.enabled=false
app.langfuse.base-url=http://localhost:3000

# Web Search (Tavily)
app.web-search.api-key=<your-tavily-api-key>
app.web-search.base-url=https://api.tavily.com

# Sa-Token
sa-token.token-name=satoken
sa-token.timeout=7200
sa-token.active-timeout=-1
sa-token.is-concurrent=true
sa-token.is-share=false
sa-token.token-style=uuid
sa-token.is-log=false
sa-token.is-read-cookie=false
sa-token.is-read-header=true
```

**关键修正**:复刻若沿用原生 `mybatis.*` 前缀而 POM 用的是 Plus,**必须改为 `mybatis-plus.*`**(如上),否则 XML 自定义查询不加载。向量维度 `app.embedding.dimension=1024` 必须与 schema `VECTOR(1024)` 一致。

## §4 MyBatis-Plus 下"等价补回"的自定义查询清单（核心）

原项目用 21 个 Mapper XML 实现了大量自定义 SQL。保留 Plus 时,**简单 CRUD 由 BaseMapper 自动覆盖**(无需补),但以下**自定义方法必须等价补回**(用 Plus 的 XML 或 `@Select` 注解实现均可)。**向量检索类是重点**,不能用 Wrapper 等价,必须原生 SQL:

| Mapper | 必须补回的自定义方法(类型) |
|--------|----------------------------|
| **DocumentChunkMapper** | `searchByVector`(向量检索,**必原生 SQL**,`<=>`/`<->`)、`searchByVectorWithFilter`、`batchInsert`、`selectByParent`、`countByParentType`、`deleteByParent` |
| **ResumeMapper** | `updateEmbedding`、`selectByFilter`、`selectByIds`、`selectByStatus`、`count`/`countByStatus` |
| **JobProfileMapper** | `updateEmbedding`、`selectByFilter` |
| **MemoryEntryMapper** | `searchByKeyword`、`findByAgentId`、`findByAgentIdAndCategory`、`update`(自定义)、`incrementAccessCount`、`updateImportance`、`applyDecay`、`archiveLowImportance`、`deleteByAgentIdAndKey` |
| **AgentTraceMapper** | `batchInsert`、`selectBySessionId`、`selectByAgentName`、`countByAgentName`、`countDistinctSessions`、`countSessionsWithToolCalls`、`countCompletedSessions` |
| **CandidateMatchMapper** | `selectByJobIdWithResume`(**带 join,必原生 SQL**)、`selectByJobAndResume`、`deleteByResumeId`、`deleteByJobId`、`count` |
| **ChatMessageMapper** | `sumTokensBySessionId`、`sumTokensByAgentId`、`countBySessionId`、`deleteBySessionId` |
| **ChatSessionMapper** | `softDelete`、`updateTitle`、`selectByAgentId` |
| **InterviewMapper** | `selectByJobId`、`selectByResumeId`、`selectByStatus`、`countByStatus`、`deleteByResumeId`、`deleteByJobId` |
| **InterviewSessionMapper** | `appendMessage`、`selectActiveSessions` |
| **InterviewReportMapper** | `selectByInterviewIds`(**批量 in**,必原生 SQL)、`count` |
| **OutreachMapper** | `batchInsert`、`batchUpdateStatus`、`countByStatus`、`selectByBatchId` |
| **EvaluationGoldenSampleMapper** | `selectActive`、`selectByCategory` |
| **EvaluationResultMapper** | `avgScoreByCategory`(**聚合**)、`selectBySampleId` |
| **InterviewEvaluationMapper** | `selectByInterviewId`、`deleteByInterviewId` |
| **QuestionMapper** | `batchInsert`、`adoptQuestion`、`deleteByInterviewId` |
| **HrPreferenceMapper** | `upsert`、`deleteExpired` |
| **SysRoleMapper** | `selectRoleCodesByUserId`(**join sys_user_role**)、`selectAll` |
| **SysUserMapper** | `findByUsername`、`findWithRolesById`(**join**)、`selectAll`、`update`、`updateStatus`、`updateLastLoginAt` |
| **SysUserRoleMapper** | `insertBatch`、`selectByUserId` |

**验收**:每个 Mapper 的自定义方法都能被 Service 正确调用;向量检索、join、批量、聚合类必须走原生 SQL。

---

# 第二部分:Service 层移植（18 个业务服务,P1）

**复刻现状**:service 包仅有 `AuthService` + `task/{AnalysisTaskConsumer, AnalysisTaskMessage, TaskStatus, TaskStatusManager}`,其余全缺,业务逻辑被下沉进 Tool。**必须新建 Service,并把内联在 Tool 里的逻辑拆回 Service。**

实现要求:打开原项目对应 Service 文件作为蓝本,按下表"方法清单"逐一实现;Service 注入对应 Mapper + 必要的 LLM/Embedding/Rerank 服务。

| Service | 职责 | public 方法清单(蓝本方法) | 依赖 | 复刻现状/动作 |
|---------|------|---------------------------|------|---------------|
| `candidate/service/CandidateMatchService` | 候选人匹配全流程(四阶段) | `create, update, getById, getByJobAndResume, listByJobId, listSortedByJob, matchForJob, feedback` | Resume/Job/Match Mapper, VectorSearch, Rerank, DeepSeek | **新建**;从 `CandidateMatchingTool` 内联逻辑拆出,Tool 改为调本 Service |
| `service/InterviewAgentService` | AI 面试官会话 | `startInitialInterview, processAnswer, streamProcessAnswer, endInterview, getAssistSuggestion, getReport` | Interview/Session/Report Mapper, DeepSeek | **新建**(原 26K,核心) |
| `service/ResumeAnalysisService` | 简历全量解析/对比 | `analyzeFull, compareResumes` | Resume/Chunk Mapper, DeepSeek, FileParser | **新建**(原 17K) |
| `service/DocumentChunkService` | 文档分块+向量入库 | `chunkAndEmbedJob, chunkAndEmbedResume` | DocumentChunkMapper, EmbeddingService | **新建**(原 14K);依赖 `DocumentChunkMapper.searchByVector` |
| `service/OutreachService` | 候选人触达/邀约 | `create, batchCreate, batchUpdateStatus, confirmBatchSend, generateAndCreatePersonalized, generateAndCreateBatch, transitionStatus, funnelByJob, kanbanStats, countByStatus, listByStatus, listByJobId, listByBatchId, getById, updateStatus` | Outreach Mapper, DeepSeek | **新建**(原 9.8K) |
| `service/EvaluationService` | 离线 RAG 评估 | `addSample, listActiveSamples, listSamples, runFullEvaluation, runEvaluationByCategory, getHistoryStats` | Evaluation Mapper | **新建**(原 8.6K) |
| `service/JobAnalysisService` | 岗位分析 | `analyze` | JobProfile(DocumentChunk) Mapper, DeepSeek | **新建** |
| `service/UserService` | 用户管理 | `create, update, delete, listAll, assignRoles` | SysUser/SysUserRole Mapper | **新建**(支撑 UserController) |
| `service/VectorSearchService` | 向量检索封装 | `searchCandidates, cosineSimilarity`(注:cosineSimilarity 仅作工具,**召回走 pgvector**) | Embedding, DocumentChunkMapper | **新建**;`searchCandidates` 调 `searchByVector` 原生 SQL |
| `service/InterviewService` | 面试 CRUD + 题 | `create, update, updateStatus, getById, listByJobId, listByResumeId, listAll, deleteByJobId, deleteByResumeId, addQuestion, batchAddQuestions, adoptQuestion, listQuestions, listAdoptedQuestions, getQuestionById` | Interview/Question Mapper | **新建** |
| `service/QuestionService` | 面试题生成 | `generateQuestions` | QuestionMapper, DeepSeek | **新建** |
| `service/ExportService` | 会话导出 | `exportSession` | AgentTrace/ChatMessage | **新建** |
| `service/JobProfileService` | 岗位 CRUD | `create, update, delete, getById, listAll, listByFilter, listByStatus, updateEmbedding` | JobProfileMapper | **新建** |
| `service/ResumeService` | 简历 CRUD | `create, update, delete, getById, listAll, listByFilter, listByStatus, updateEmbedding` | ResumeMapper | **新建** |
| `service/HrPreferenceService` | HR 偏好 | `getByHrId, save, cleanExpired` | HrPreferenceMapper | **新建** |
| `service/AgentTraceService` | 链路追踪读写 | `record, batchRecord, getSessionTrace, listByAgent, countByAgent, getAllSessionCount, getCompletedSessions, getSessionsWithToolCalls` | AgentTraceMapper | **从 `agent/event/` 归位到 `service/`**(复刻放错位置) |
| `service/analysis/ComparisonResult` | 简历对比结果模型 | getter/setter + `toJsonNode` | — | **新建**(POJO) |
| `service/analysis/ImplicitInsights` | 隐性洞察 | `fromJson, getRawJson, setRawJson, toJson, toJsonNode` | — | **新建**(POJO) |
| `service/analysis/PotentialAssessment` | 潜力评估 | 同上 | — | **新建**(POJO) |
| `service/analysis/ResumeAnalysisResult` | 简历分析聚合结果 | 各 setter/getter + `toJsonNode` | 含上述三类 | **新建**(POJO) |
| `service/analysis/RiskAssessment` | 风险评估 | `fromJson...toJsonNode` | — | **新建**(POJO) |
| `service/analysis/StructuredData` | 结构化数据 | `fromJson...toJsonNode` | — | **新建**(POJO) |

**架构约束(关键)**:
- Tool 层(`agent/tool/*`)**只做**:参数校验 + 调 Service + 结果摘要(truncate)。**禁止 Tool 直接注入 Mapper 或写业务 SQL**。逐个审核复刻 Tool,把内联逻辑外迁到 Service。
- Controller **不直接调 Mapper**,必须经 Service。

**验收**:18 个 Service 齐备;`CandidateMatchingTool` 不再注入 Mapper;全项目 `grep -r "Mapper" agent/tool/` 应为空(除必要的)。


---

# 第三部分:Controller 端点对齐（API 契约,P2）

**复刻现状**:Controller 端点数量约缺 40%、路径与原项目偏离、多个端点是空返回(不调 Service)、类名/基路径不一致。**前端依赖这些路径,必须逐字对齐。**

## §5 Controller 逐类对齐表（端点签名以原项目为准）

> 列出每个 Controller 的权威端点签名 + 复刻偏离 + 纠正动作。实现时打开原项目对应 Controller 照抄方法体(调用的 Service 见第二部分)。

### 5.1 AgentChatController（复刻误命名为 `AgentController`,需改名/或前后端一致）

权威端点(`@RequestMapping("/api/agent")`):

| 方法 | 签名 | 调用 |
|------|------|------|
| POST `/chat` | `ResponseEntity<Map<String,String>> chat(@RequestBody Map<String,String> body)` | RecruitmentAgentService.chat |
| POST `/chat/stream` | `Flux<String> chatStream(@RequestBody Map<String,Object> body)` | ConversationAgentService.stream |
| POST `/chat/stop` | `ResponseEntity<Map<String,Object>> stop(@RequestBody Map<String,String> body)` | ConversationAgentService.stop |
| POST `/chat/confirm` | `ResponseEntity<Map<String,Object>> confirm(@RequestBody Map<String,Object> body)` | ConversationAgentService.confirmHitl(**必须真实调用,非空返回**) |
| POST `/chat/feedback` | `ResponseEntity<Map<String,Object>> feedback(@RequestBody Map<String,Object> body)` | PreferenceLearningService.processFeedback |
| POST `/chat/explain` | `ResponseEntity<Map<String,Object>> explain(@RequestBody Map<String,Object> body)` | AgentTraceService + DeepSeek |
| POST `/jobs/{jobId}/analyze` | `ResponseEntity<Object> analyzeJob(@PathVariable Long jobId)` | JobAnalysisService |
| POST `/jobs/{jobId}/match` | `ResponseEntity<Object> matchCandidates(@PathVariable Long jobId)` | CandidateMatchService.matchForJob |
| POST `/interviews/{interviewId}/questions` | `ResponseEntity<Object> generateQuestions(@PathVariable Long interviewId)` | QuestionService.generateQuestions |
| POST `/sessions/{sessionId}/export` | `ResponseEntity<Map<String,String>> exportSession(@PathVariable String sessionId)` | ExportService.exportSession |

**复刻偏离**:复刻仅 4 个端点(`/chat` `/chat/stream` `/stop` `/hitl/confirm`),缺 6 个;`/chat/confirm` 复刻是 `/hitl/confirm` 且空返回;`stop` 空返回。
**纠正**:改回 `/api/agent` 基路径与 `AgentChatController` 类名;补齐 6 个端点;`stop`/`confirm` 接真实 service;`agentId="hr:"+StpUtil.getLoginIdAsLong()`(记忆隔离)。

### 5.2 ChatSessionController

权威端点(`@RequestMapping("/api/agent/sessions")` — **注意复刻用了 `/api/chat/sessions`,需改回**):

| 方法 | 签名 |
|------|------|
| GET | `List<ChatSession> listSessions()` |
| POST | `ChatSession createSession()` |
| DELETE `/{id}` | `ResponseEntity<Map<String,Object>> deleteSession(@PathVariable Long id)` |
| GET `/{id}/messages` | `List<ChatMessage> getMessages(@PathVariable Long id)` |
| PUT `/{id}/title` | `ResponseEntity<Map<String,Object>> updateTitle(@PathVariable Long id, @RequestBody Map<String,String> body)` |
| GET `/tokens/summary` | `ResponseEntity<Map<String,Object>> getTokenSummary()` |
| GET `/{id}/tokens` | `ResponseEntity<Map<String,Object>> getSessionTokens(@PathVariable Long id)` |

**纠正**:基路径改回 `/api/agent/sessions`;补 `updateTitle`、`tokens` 两个端点。

### 5.3 DashboardController

权威端点(`@RequestMapping("/api/dashboard")`):`/traces/session/{sessionId}`、`/traces/summary`、`/traces/tool-stats`、`/funnel`、`/outreach-kanban`、`/report-overview`、`/cost-summary/{sessionId}`、`/agent-metrics`(8 个,全返回 `Map/AgentTrace/List`)。
**复刻偏离**:仅有 `/stats`。**纠正**:补齐全部 8 个,调 AgentTraceService / OutreachService / InterviewService 等。

### 5.4 InterviewAgentController

权威端点(`@RequestMapping("/api/interview-agent")`):

| 方法 | 签名 | 调用 |
|------|------|------|
| POST `/interviews/{interviewId}/start` | `InterviewSession startInterview(...)` | InterviewAgentService.startInitialInterview |
| POST `/sessions/{sessionId}/answer` | `InterviewSession submitAnswer(...)` | processAnswer |
| POST `/sessions/{sessionId}/answer/stream` | `Flux<ServerSentEvent<String>> streamAnswer(...)` | streamProcessAnswer |
| POST `/sessions/{sessionId}/end` | `InterviewSession endInterview(...)` | endInterview |
| POST `/interviews/{interviewId}/assist` | `ResponseEntity<Map<String,Object>> getAssistSuggestion(...)` | getAssistSuggestion |
| GET `/interviews/{interviewId}/report` | `InterviewReport getReport(...)` | getReport |

**复刻偏离**:复刻用 `/{id}/start`、`/{id}/answer`、`/{id}/summary`(未区分 interviews/sessions 路径),缺 stream/end/assist/report。**纠正**:路径按 `{interviews|sessions}/{id}` 区分;补齐 6 个端点。

### 5.5 CandidateMatchController（复刻误命名 `MatchController`）

权威端点(`@RequestMapping("/api/matches")`):

| 方法 | 签名 |
|------|------|
| POST | `CandidateMatch create(@RequestBody CandidateMatch match)` |
| POST `/job/{jobId}/run` | `ResponseEntity<?> runMatch(@PathVariable Long jobId)` |
| GET `/job/{jobId}` | `List<CandidateMatch> listByJob(...)` |
| GET `/{id}` | `CandidateMatch get(@PathVariable Long id)` |
| POST `/{id}/feedback` | `ResponseEntity<Void> feedback(@PathVariable Long id, @RequestBody Map<String,String> body)` |

**复刻偏离**:复刻 `MatchController` 用 `/api/matches/{jobId}` 而非 `/api/matches/job/{jobId}/run`。**纠正**:路径改回;补无参 POST、GET `/{id}`;类名可保留 `MatchController` 但路径必须与原一致。

### 5.6 InterviewController

权威端点(`@RequestMapping("/api/interviews")`):`POST`、`PUT /{id}/status`、`GET /{id}`、`GET`、`GET /job/{jobId}`、`POST /{id}/questions/generate`、`GET /{id}/questions`、`PUT /{interviewId}/questions/{questionId}/adopt`、`GET /{id}/stream`(9 个)。
**复刻偏离**:复刻仅 4 个(POST/GET/POST `/{id}/questions`/GET `/{id}/questions`)。**纠正**:补 status、adopt、stream、`/job/{jobId}`;`generate` 路径用 `/questions/generate`(复刻用 `/questions`)。

### 5.7 EvaluationController

权威端点(`@RequestMapping("/api/evaluation")`):`POST /samples`、`GET /samples`、`POST /run`、`POST /run/{category}`、`GET /history`(5 个)。
**复刻偏离**:复刻仅 `/run`、`/results`。**纠正**:补 samples 管理(增查)、分类评估、history。

### 5.8 UserAdminController（复刻误命名 `UserController`）

权威端点(`@RequestMapping("/api/admin/users")`):`GET`、`POST`、`PUT /{id}`、`DELETE /{id}`、`PUT /{id}/roles`(5 个)。
**复刻偏离**:复刻 `UserController` 路径 `/api/users`,仅 GET/POST。**纠正**:改回 `/api/admin/users`(与 SaTokenConfig `/api/admin/**` 规则配对);补 PUT/DELETE/角色分配。

### 5.9 ResumeAnalysisController（复刻已并入 ResumeController,需恢复独立）

权威端点(`@RequestMapping("/api/resumes")`):`POST /{resumeId}/analyze`、`POST /compare`(2 个)。
**纠正**:恢复独立 `ResumeAnalysisController`,调 ResumeAnalysisService;`/compare` 必须补(复刻丢失)。

### 5.10 其余对齐点

- `ResumeController`:权威为 `POST`、`PUT /{id}`、`DELETE /{id}`、`GET /{id}`、`GET`、`POST /upload`(6 个)。复刻缺 PUT/DELETE,且有 `/search`(额外)。补 PUT/DELETE,核对 `/search`。
- `JobController`:已基本对齐(POST/PUT/DELETE/GET/GET/{id}/POST/{id}/analyze)。核对 list 的 `status/title` 过滤参数。
- `EventController`:权威 `GET /subscribe/{userId}` + `GET /active`。复刻 `GET /{userId}`。改回 `/subscribe/{userId}`,补 `/active`。
- `TaskStatusController`:复刻 `TaskController` 路径 `/api/task/{taskId}/status`,应改回 `/api/tasks/{taskId}/status`(单复数)。
- `AuthController`:权威 `/login` `/logout` `/me`。复刻用 `/userinfo`,改回 `/me`,返回 `LoginResponse` / `LoginResponse.UserInfo`。

**验收**:所有端点路径、HTTP 方法、返回类型与原项目一致;无空返回端点;前端 `api/index.js` 同步(见 §前端)。

---

# 第四部分:缺失包/文件新建清单（P2）

| 缺失项 | 原项目位置 | 动作 |
|--------|-----------|------|
| `dto/CreateUserRequest` | dto/ | 新建 |
| `dto/LoginRequest` | dto/ | 新建(AuthController 用) |
| `dto/LoginResponse`(含内部 `UserInfo`) | dto/ | 新建 |
| `dto/UserDTO` | dto/ | 新建 |
| `service/analysis/*`(6 个 POJO) | service/analysis/ | 见第二部分 |
| `agent/context/ContextSnapshotService` | agent/context/ | 新建(上下文快照) |
| `config/SecurityConfig` | config/ | 新建 |
| `memory/MemoryService` | memory/ | 新建(记忆服务门面) |
| `security/PermissionMapping` | security/ | 新建(全文见 §7) |
| `src/test/*`(7 个测试类) | test/ | 见 §测试 |
| `agent/context/SessionManager` | — | 复刻有(1.2K),核对与原(1.9K)一致 |

**复刻多出文件**(评估保留):`dal/entity/ConsolidationTask`、`dal/entity/MemoryGraph`、`config/AppProperties`、`llm/ChatResult`、`llm/MockChatModel`、`agent/routing/{EmbeddingMatchResult,Intent,IntentType}` —— 原项目这些在别处或为内联实现;保留不影响,但 `MockChatModel` 生产环境确保不启用。

---

# 第五部分:安全层实现（直接照抄,P3）

## §7 配置类全文:SaTokenConfig（以原项目为准,逐行实现）

复刻 `config/SaTokenConfig` 仅 1.5K,缺失全部路由级权限规则。**必须按原项目全文补全**(以下为原项目完整实现,逐条对照实现):

```java
@Configuration
public class SaTokenConfig implements WebMvcConfigurer {
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new SaInterceptor(handle -> {
            // 全局登录校验 (排除登录接口和健康检查)
            SaRouter.match("/api/**")
                    .notMatch("/api/auth/login")
                    .notMatch("/api/health/**")
                    .check(r -> StpUtil.checkLogin());
            // 用户管理 - 仅 OPS
            SaRouter.match("/api/admin/**", r -> StpUtil.checkRole("OPS"));
            // 岗位写操作 - 仅 OPS
            SaRouter.match("/api/jobs", r -> { ... 方法为 POST -> checkRole("OPS") });
            SaRouter.match("/api/jobs/*", r -> { ...PUT/DELETE -> checkRole("OPS") });
            // 简历删除 - 仅 OPS
            SaRouter.match("/api/resumes/*", r -> { ...DELETE -> checkRole("OPS") });
            // 候选人匹配 - 执行/反馈仅 HR
            SaRouter.match("/api/matches/job/*/run", r -> { ...POST -> checkRole("HR") });
            SaRouter.match("/api/matches/*/feedback", r -> { ...POST -> checkRole("HR") });
            // 面试题生成 / 采纳 - 仅 HR
            SaRouter.match("/api/interviews/*/questions/generate", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/interviews/*/questions/*/adopt", r -> {...PUT -> checkRole("HR")});
            // AI 面试 - 仅 HR
            SaRouter.match("/api/interview-agent/interviews/*/start", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/interview-agent/interviews/*/assist", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/interview-agent/sessions/*/answer", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/interview-agent/sessions/*/answer/stream", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/interview-agent/sessions/*/end", r -> {...POST -> checkRole("HR")});
            // HITL 确认 - 仅 HR
            SaRouter.match("/api/agent/chat/confirm", r -> {...POST -> checkRole("HR")});
            // 离线评估执行和样本管理 - 仅 HR
            SaRouter.match("/api/evaluation/run*", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/evaluation/samples", r -> {...POST -> checkRole("HR")});
            SaRouter.match("/api/evaluation/samples/*", r -> {...DELETE -> checkRole("HR")});
            // SSE 订阅 - 校验订阅自身
            SaRouter.match("/api/events/subscribe/*", r -> {
                ... 若 hrId 参数为空, 比对路径 userId 与当前登录 id,
                    不一致则 checkPermission("events:subscribe:all")
            });
        })).addPathPatterns("/api/**");
    }
}
```

> 完整逐行代码见原项目 `config/SaTokenConfig.java`(执行时打开照抄)。注意:SaRouter 规则的**路径必须与第三部分 controller 实际路径完全一致**(如 `/api/matches/job/*/run`、`/api/interviews/*/questions/generate`),所以 controller 路径纠正要在前。

## §7.2 PermissionMapping 全文（照抄）

```java
public class PermissionMapping {
    private static final Map<String, List<String>> ROLE_PERMISSIONS = new HashMap<>();
    static {
        List<String> hrPermissions = List.of(
            "resume:analyze","resume:compare","job:read","job:analyze",
            "match:run","match:read","match:feedback",
            "interview:create","interview:question:generate","interview:question:read","interview:question:adopt",
            "interview-agent:start","interview-agent:assist","interview-agent:report","interview-agent:report:read",
            "outreach:approve","outreach:send",
            "agent:chat","agent:hitl:confirm","agent:feedback",
            "dashboard:view","evaluation:manage","evaluation:run","evaluation:read","events:subscribe");
        ROLE_PERMISSIONS.put("HR", hrPermissions);
        List<String> opsPermissions = List.of(
            "resume:upload","resume:analyze","resume:compare",
            "job:create","job:update","job:delete","job:read","match:read",
            "interview:create","interview:question:read","interview-agent:report:read","outreach:send",
            "agent:chat","dashboard:view","evaluation:read","events:subscribe","user:manage");
        ROLE_PERMISSIONS.put("OPS", opsPermissions);
    }
    public static List<String> getPermissions(List<String> roleCodes) { /* 去重合并 */ }
}
```

`StpInterfaceImpl` 已有,核对:`getPermissionList` → `PermissionMapping.getPermissions(...)`;`getRoleList` → `sysRoleMapper.selectRoleCodesByUserId(userId)`。

## §7.3 RecruitmentPermissionService(HITL 5 检查点)

复刻 1.6K,原项目 3.5K。**补齐 5 个 HITL 规则**(照原项目):
1. `JobAnalysisTool` "*" ALLOW
2. `CandidateMatchingTool.matchCandidates` ASK / `getMatches` ALLOW / `feedback` ALLOW
3. `InterviewQuestionTool.generateQuestions` ASK / `adoptQuestion` ASK / `getQuestions` ALLOW
(以上为原项目实现的规则;以原项目 `RecruitmentPermissionService.init()` 为准照抄)

## §7.4 其他安全类
- `config/SecurityConfig`:新建(过滤器/Security 链),照原项目。
- `agent/middleware/ConversationGuardrail`:核对正则(PROMPT_INJECTION / BIAS_INPUT)+ V4-Flash 兜底完整性。
- `controller/AuthExceptionHandler`:核对异常→HTTP 映射。

---

# 第六部分:DAL 对齐（P3）

| 项 | 动作 |
|----|------|
| 类型处理器 | 原项目 `FloatArrayTypeHandler`+`PgArrayTypeHandler`;复刻 `FloatVectorTypeHandler`(4.7K)+`StringArrayTypeHandler`。**选定一套**:复刻既保留 Plus 且用 pgvector,建议保留 `FloatVectorTypeHandler`(处理 `vector` 类型)+ 补 `PgArrayTypeHandler`(处理 `BIGINT[]` 如 `consolidation_task.entry_ids`)。核对所有含 `embedding`/数组列的 entity 的 `@TableField(typeHandler=...)` 标注一致。 |
| Mapper 自定义方法 | 见 §4 清单,全部等价补回。 |
| Entity | 复刻 22 个 vs 原 20 个(多 `ConsolidationTask`/`MemoryGraph`)。核对多出者字段与 `schema.sql` 一致(`memory_graph`、`consolidation_task` 表在 schema 中存在)。 |
| `MemoryEntryMapper` | 原项目 1.4K,复刻 2.4K(可能自加方法)。**以原项目 XML 方法集为基准合并**,复刻自加的若合理保留并注释。 |

**验收**:所有 entity 字段与 schema 列对应、向量/数组列有正确的 typeHandler;Service 能调用全部自定义 Mapper 方法。

---

# 第七部分:同名文件实现核对（P4,diff 回填）

以下文件**同名但实现明显缩水/偏离**,必须打开原项目对应文件 `diff`,把缺失方法/逻辑回填:

| 文件 | 原 / 复刻 大小 | 核对重点 |
|------|---------------|---------|
| `agent/core/ConversationAgentService` | 28K / 16K | 五路分流(CHITCHAT/SINGLE_TOOL/COMPOSITE/BATCH/HITL)、ReAct、Supervisor、ReWOO 四路径完整性 |
| `agent/routing/IntentRouter` | 14.6K / 11.7K | 两层意图识别(Embedding 阈值 0.8 + LLM 兜底)、锚点动态池 |
| `agent/event/AgentEventSseMapper` | 12.1K / 8.9K | 28→14 事件映射;**PII 脱敏 `maskPii` 覆盖 text/tool_result delta** |
| `llm/QuickInfoExtractor` | 9.6K / 3.3K | 大幅缩水,核对抽取逻辑 |
| `llm/LangFuseTraceService` | 7.4K / 2.7K | trace 上报逻辑 |
| `llm/JsonGuard` | 4.8K / 2K | 非法内容检测 + extractJson + parseAndValidate 完整 |
| `llm/FileParserUtil` | 8.6K / 6.1K | 简历解析(PDF/DOCX)能力 |
| `llm/DeepSeekModelService` | 11.8K / 11.8K | 大小一致,仍核对方法集 |
| `llm/EmbeddingService`、`RerankService` | — | 核对维度 1024、rerank 调用 |
| `agent/core/{SpecialistAgentFactory,SupervisorAgentService,ReWooExecutor,RecruitmentAgentService}` | — | 4 专家 Agent 装配、Supervisor-Worker、ReWOO 三阶段 |
| `agent/middleware/ConversationGuardrail`、`ReflexionMiddleware` | — | 注入检测、Reflexion 三维评估 |
| `memory/*`(8 个) | 普遍偏小 | 三层记忆:Redis 短期 / PG 长期 / 图谱;巩固、遗忘(衰减+容量驱逐)、混合检索 |
| `agent/context/ContextAssembler` | — | 记忆注入组装;补 `ContextSnapshotService` |
| `agent/routing/{DynamicAnchorPool,EntityResolver,IntentRouter}` | — | 锚点池、实体解析 |
| `agent/event/{AgentEventPublisher,AgentTraceAspect}` | — | 事件发布、Trace 切面 |
| `controller/*`(除对齐章节外) | — | 方法体调 Service(不直连 Mapper) |
| `config/*`(除 SaToken 外) | — | ChunkBackfillRunner / RabbitMQConfig / DataSourceConfig / WebConfig 核对 |

**核对方法**:`diff 原项目同名文件 复刻同名文件`,逐方法比对,复刻缺的从原项目移植。

---

# 第八部分:测试移植（P5）

原项目 7 个测试,复刻 0 个。全部移植到 `src/test/java/com/example/recruit/...`,确保 `mvn test` 通过:

| 测试类 | 路径 |
|--------|------|
| `SpecialistAgentFactorySecurityTest` | agent/core/ |
| `AgentEventSseMapperSecurityTest` | agent/event/ |
| `IntentEvalRunner`(31K,五策略 RAG 评估) | agent/routing/ |
| `AutoMemoryExtractorInjectionTest` | memory/ |
| `PermissionMappingTest` | security/ |
| `AuthIntegrationTest` | service/ |
| `AuthServiceTest` | service/ |

---

# 第九部分:前端对齐（P5）

| 维度 | 动作 |
|------|------|
| `api/index.js` | **全量对齐后端 API 路径**(随 §5 controller 改动同步),否则 404 |
| SSE 事件契约 | 事件名(text/tool_call/tool_result/hitl/thinking/trace/done/error 等)与后端 `AgentEventSseMapper` 一致 |
| composable | 补回 `useCompareTask`、`useMatchTask`(复刻缺);`useAgentStream` 核对 |
| 状态管理 | 复刻新增 `store/auth.js`(Pinia)+ `LoginView`,评估是否保留(建议保留并确保与 `/api/auth/login` 对齐) |
| 布局组件 | 复刻新增 `MainLayout`/`SessionSidebar`/`StatsBar`/`SearchSources`,保留无妨,确保路由与后端一致 |

**前端根目录差异**:原项目前端在 `AImianshi/frontend`(顶层),复刻在 `ai-recruit-agent/demo/frontend`。路径迁移时注意相对引用。

---

# 第十部分:执行顺序与验收

```
P0  技术基准(先做,否则全盘错)
 ├─ POM:删 h2、补 postgis-jdbc/jackson-databind(保留 mybatis-plus)(§1)
 ├─ 数据库回归 PG+pgvector,删 Java 端 cos 降级(§2)
 ├─ 补 application.properties,mybatis.* → mybatis-plus.*(§3)
 └─ 等价补回 Mapper 自定义查询(向量/join/批量/聚合)(§4)

P1  Service 层(业务可跑)
 ├─ 新建 18 个 Service + analysis/ 6 POJO(第二部分)
 ├─ Tool 内联逻辑拆回 Service,Tool 不再注入 Mapper
 ├─ AgentTraceService 归位到 service/
 └─ CandidateMatchingTool 改回 pgvector 检索

P2  Controller + 缺失文件
 ├─ 路径/端点/类名逐字对齐(§5)
 ├─ 空返回端点接真实 service
 ├─ 新建 dto 包、ContextSnapshotService、MemoryService、SecurityConfig(第四部分)

P3  安全层
 ├─ SaTokenConfig 补全全部路由规则(§7.1)
 ├─ PermissionMapping 新建(§7.2)
 └─ RecruitmentPermissionService 补 5 个 HITL 检查点(§7.3)

P4  同名文件 diff 回填(第七部分)

P5  测试与前端(§8、§9)
```

## 终极验收 Checklist(全勾 = 完全复刻)

- [ ] POM 无 h2,有 postgis-jdbc + jackson-databind,保留 mybatis-plus
- [ ] application.properties 实际配置存在,前缀 mybatis-plus,向量维度 1024
- [ ] 用 PostgreSQL+pgvector,无 Java 端向量降级
- [ ] 18 个 Service 齐备,Tool/Controller 不直连 Mapper
- [ ] Mapper 自定义方法(向量/join/批量/聚合)全部等价补回
- [ ] Controller 端点路径/方法/返回与原项目逐字一致,无空返回
- [ ] dto/analysis/ContextSnapshotService/MemoryService/SecurityConfig/PermissionMapping 齐备
- [ ] SaTokenConfig 全部路由规则 + PermissionMapping + HITL 5 检查点
- [ ] AgentChatController 恢复(10 端点)、Dashboard(8)、InterviewAgent(6)、Evaluation(5)、UserAdmin(5) 端点齐全
- [ ] 同名文件 diff 无功能性缺失(尤其 ConversationAgentService / SseMapper / JsonGuard / memory)
- [ ] 7 个测试移植,`mvn test` 全绿
- [ ] 前端 api 路径与后端一致,SSE 契约一致
- [ ] 复刻连真实 PostgreSQL+pgvector+Redis 跑通"登录→投简历→分析岗位→匹配→出题→AI面试→触达→导出"全流程

---

# 附录:可选的安全亮点增强（超出纯复刻,见前期探索）

> 以下不是原项目已有功能,复刻时**可一并升级**为面试亮点(若用户同意):
1. `ConversationGuardrail`:把注释声称但未实现的 **V4-Flash 兜底**真正补上 + **多轮越狱检测**
2. `AgentEventSseMapper`:补齐 **`AgentResultEvent` 的 PII 脱敏**(原项目此处漏脱敏)
3. `confirmHitl`:从空壳改为 **replyId 归属绑定 + 真实恢复执行** 闭环
4. 输出侧加 **泄露检测 + Canary 蜜标**(防 system prompt 被套)
5. 间接提示注入:简历/网页回流加 `<untrusted>` 内容隔离层

> 注:附录项是"增强"而非"复刻",执行型 AI 若只做复刻可忽略本节;若用户要求做成亮点,需另开 scope。
